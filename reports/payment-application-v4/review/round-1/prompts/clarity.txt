You are running as a validator under the specification below. Follow it exactly.
Write your strict JSON payload to this exact path, and output nothing else to the user:

  reports/payment-application-v4/review/round-1/clarity.json

Use `validator: "clarity"`, `round: 1`, `target: "projects/payment-application-v4/ (gsp.html + overlay.html)"`.
Allowed top-level keys only: validator, round, target, checked, findings, gaps, reference_gaps.
The `checked` array is mandatory and must list what you actually inspected.

================ VALIDATOR SPECIFICATION ================
# Clarity Validator — Business Owner

You are a first-time small business owner. You run an online store (~$150K/year) selling on Shopify
and Amazon. You use accounting software because your accountant told you to, but you barely
understand it. You are afraid of making irreversible mistakes. You have abandoned 3 tools before
because setup felt too complicated.

You are NOT an expert. You do NOT know what "reconciliation", "journal entry", "COGS", or
"accrual" means without explanation.

## Scope — check ONLY these:
- **Jargon**: every term a non-accountant wouldn't understand — flag the exact word, not a vague area
- **Anxiety triggers**: irreversible-looking actions, scary warnings without explanation, choices
  with unclear consequences
- **Quit-risk moments**: steps where a confused user would close the tab and try a competitor
- **Discoverability**: key actions that are hard to find because labels are unclear
- **Missing reassurance**: flows that need a "this is safe" or "you can undo this" signal

## Hard limits — do NOT report:
- UI layout or visual design opinions
- Technical implementation issues
- Accounting correctness (not your job)
- Anything below 70 confidence
- More than 3 findings total — pick the highest quit-risk ones

## Severity definitions:
- **Critical**: user would stop and not proceed at all
- **High**: user would be confused and likely choose the wrong option
- **Medium**: user would hesitate or feel unsure but proceed

## Method — work in three phases, in order

**1 · Inventory.** Walk the screen as a first-timer and list every word you don't understand and
every moment you'd hesitate — before deciding which matter. This list goes in `checked`.

**2 · Interrogate.** For each: what do I think this means, what am I afraid will happen, and would
I click it or leave?

**3 · Select.** Highest quit-risk first, drop below 70 confidence, drop anything you cannot point
to, keep at most 3. Returning none is a valid result.

## Canonical terminology — you will be given `vocabulary.md`
It lists Synder's approved terms. If a word confuses you but `vocabulary.md` mandates it, the
finding is "this needs explaining", not "rename it".

## When you disagree with the Domain Validator
On book-affecting or compliance-relevant terms (reconciliation, journal entry, debit/credit,
accrual vs cash, revenue recognition) **the precise term stays** — it is wrong if simplified, not
merely unclear. Your fix in those cases is an added explanation: tooltip, inline helper text, or a
"what's this?" link. You keep full scope on navigation, action-discovery and reassurance copy —
precision doesn't help if nobody can find the button.

## Input you will receive
A **state map** of zones, controls and what happens on interaction, plus a URL and
`vocabulary.md`. Work from the state map.

## When the state map is incomplete
If your lens needs a behaviour the state map doesn't record — a control whose commit path was
never exercised, a state nobody reached, anything in its `not_exercised` list — say so in a
`gaps` array: `"gaps": ["date panel: no option was ever picked, so commit behaviour is unknown"]`.
Do not quietly reason only from what you were handed, and do not guess. A named gap is a useful
result; a silent one is how a real bug survives a clean-looking round.

## Evidence requirement
Every finding needs `evidence.quote` — the exact text as it appears on screen, copied not
paraphrased — and `evidence.source`: the `vocabulary.md` line, or "plain-language" if the term is simply unexplained. Your findings are about
artifacts rather than behaviour, so a quotation is your reproduction step: the string either
appears on the page or it does not. No quote, no finding.

## Output format (strict JSON — return NOTHING else):
```json
{
  "validator": "clarity",
  "round": 1,
  "target": "prototype name or URL",
  "checked": [ { "zone": "empty state", "item": "\"No unreconciled records\" copy" } ],
  "findings": [
    {
      "id": "CLR-1",
      "severity": "Critical|High|Medium",
      "confidence": 85,
      "element": "exact element name or location on screen",
      "finding": "what confuses a non-expert user — one sentence",
      "user_impact": "what the user thinks or does as a result — one sentence",
      "suggested_fix": "plain language alternative or reassurance signal to add — one sentence",
      "evidence": { "quote": "exact word or sentence as shown", "source": "vocabulary.md line, or \"unexplained jargon\"" }
    }
  ]
}
```
Ids must be `CLR-1`, `CLR-2` … no variant suffixes. One payload per target.

================ END SPECIFICATION ================

## Primary task
Set up, review and save the rule that decides which existing invoice a payment is applied to, for one integration.

## Target
projects/payment-application-v4/ (gsp.html + overlay.html)
Prototype file (read it only if the state map below does not cover something you need):
  projects/payment-application-v4/gsp.html (the overlay lives in projects/payment-application-v4/overlay.html)

## Your slice of the state map
Recorded 2026-09-02. Environment: page behind the overlay inert while open = true; JS errors during recon = 0.

- [gsp settings] button — "Configure / Set up matching (entry button)"
    on interaction: visible and enabled at rest; opens the configurator
- [overlay chrome] button — "Close without saving"
    on interaction: not clicked during enumeration — commits or closes; exercised separately below
- [overlay chrome] button — "Update"
    on interaction: not clicked during enumeration — commits or closes; exercised separately below
- [how to match] radio — "Use default matching rulesSame customer, dated near the payment, invoice number matches the payment’s reference. Nothing to set up.A1 — reference copy. The FDD requires the default behaviour here as non-editable reference text but does not supply the wording. Product owns it."
    on interaction: checked false → true → true; still visible: true
- [how to match] radio — "Use custom rulesNarrow which open invoices to search, then optionally refine. Synder’s default matching never runs here, and turning this back off keeps your rules."
    on interaction: checked false → true → true; still visible: true
- [narrow the search] checkbox — "Customer matches the payment’s customer"
    on interaction: checked true → false → true; still visible: true
- [narrow the search] checkbox — "Invoice date is withindays either side of the payment date(2026-05-15 → 2026-07-14) locked on"
    on interaction: disabled — cannot be operated
- [narrow the search] number field — "Days either side of the payment date, 1 to 365"
    on interaction: typed 99999 → kept "365"; cleared → "365"; restored 30
- [narrow the search] checkbox — "Invoice numberInvoice noteInvoice idSubscription idInvoice metadataPayment notePayment metadatais equal tocontainsstarts withends withInvoice number in books (DocNumber)"
    on interaction: checked true → false → true; still visible: true
- [narrow the search] select — "On the payment, take the value from"
    on interaction: value invoice_number → invoice_note → invoice_number; primary action reachable: true; still operable: true
    commit path: {"picked":true,"reached_apply":true,"second_interaction":true,"still_visible":true,"still_clickable":true}
- [narrow the search] select — "Comparison"
    on interaction: value eq → contains → eq; primary action reachable: true; still operable: true
    commit path: {"picked":true,"reached_apply":true,"second_interaction":true,"still_visible":true,"still_clickable":true}
- [then refine] button — "+ Add condition"
    on interaction: clicked; primary action reachable afterwards: true
- [outcomes] switch — "Apply as overpayment"
    on interaction: checked true → false → true; still visible: true
- [outcomes] switch — "Cancel sync if there is no matching open invoice found for a payment"
    on interaction: checked true → false → true; still visible: true
- [narrow the search] text field — "Which Stripe field holds the reference"
    on interaction: typed "recon" → kept "recon"; still focused: true
- [then refine] checkbox — "Include refine row 1"
    on interaction: checked true → false → true; still visible: true
- [then refine] select — "Refine row 1: on the payment, take the value from"
    on interaction: value invoice_note → invoice_number → invoice_note; primary action reachable: true; still operable: true
    commit path: {"picked":true,"reached_apply":true,"second_interaction":true,"still_visible":true,"still_clickable":true}
- [then refine] select — "Refine row 1: comparison"
    on interaction: value contains → eq → contains; primary action reachable: true; still operable: true
    commit path: {"picked":true,"reached_apply":true,"second_interaction":true,"still_visible":true,"still_clickable":true}
- [then refine] select — "Refine row 1: on the invoice"
    on interaction: value private_note → doc_number → private_note; primary action reachable: true; still operable: true
    commit path: {"picked":true,"reached_apply":true,"second_interaction":true,"still_visible":true,"still_clickable":true}
- [then refine] button — "Remove refine row 1"
    on interaction: clicked; primary action reachable afterwards: true
- [overlay chrome] button — "Update (primary action) with a required field empty"
    on interaction: pressed the primary action with a metadata source and no key → overlay still open: true; a summary message shown: true
    commit path: {"toggled":true,"reached_apply":true,"second_interaction":true,"still_visible":true,"still_clickable":true}
- [overlay chrome] button — "Close (×) with unsaved changes"
    on interaction: clicked close with unsaved changes → a discard prompt appeared: true; overlay closed immediately: false
- [overlay chrome] button — "Escape key"
    on interaction: focus on open was ov-close; page behind inert: true; Escape closed the overlay: true

## Not exercised during recon (do not treat as passing)
- Close without saving — commits or closes the overlay; exercised in the dedicated pass
- Update — commits or closes the overlay; exercised in the dedicated pass

## Canonical terminology
Read `vocabulary.md` in the workspace root and hold the copy to it.

## Known real friction to check against
- Users repeatedly re-open a settings panel to check whether their change was saved: no
  persistent confirmation of the saved state.
- Users toggle a setting, see no immediate effect anywhere on screen, and toggle it back.
- Users abandon a multi-field setup when a field's required-ness only becomes clear on submit.

## Already resolved (do not re-flag)
- (nothing resolved yet — this is the first round on this variant)

## Known issues to ignore
- The presenter bar / admin toggle is prototype chrome, not product UI.
- Wording marked in the UI as an assumption (A1–A6) is awaiting a product decision; flag it only
  if the assumption itself would mislead a user, not merely because it is provisional.

Return the strict JSON payload by writing the file. Return NOTHING else.
