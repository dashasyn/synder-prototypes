# 2026-08-12

## Snapshot (23:00 Vilnius / 20:00 UTC, scheduled end-of-day review)

**No work session today.** No commits, no prototype edits, no messages from Ignat.
The only files touched were background automation:

- `03:00` — dreaming pass wrote `memory/dreaming/{light,deep,rem}/2026-08-12.md`, updated `DREAMS.md`
  and `memory/.dreams/events.jsonl`, and rotated the 08-11 session corpus
- `03:00` — MEMORY.md promotion block swapped: the 2026-08-11 promotions (ETC notifications table
  diagnosis, the 62% empty-cells figure) were replaced by 2026-08-06 promotions
  (Reports/Financials template-description audit: verdict + the 5 copy bugs + project state)
- `07:09` — `memory/morning-ideas.json` appended
- `16:59–17:00` — clawmetry metrics/fleet DB

Nothing was lost — the promotion block rotates; the ETC material lives in `PROJECTS.md`
and `memory/2026-08-05.md`.

## Where things stood at end of 2026-08-11 (carried forward unchanged)

**Filtering Options — v8 shipped and verified.** Last commit `7ab647e` ("V7: status segments;
shared segment component"). 140 jsdom + 85 Chromium checks green, mirror byte-identical,
both live URLs verified. Seven variants, all Apply-gated.
Yesterday's two lessons, still the most portable things from the week:
1. When a layout feels wrong, check what the code *computes* before arguing from convention —
   `countForStatuses` settled the filters-above-segments question in one grep, against my
   "tabs above filters, like GitHub" instinct.
2. Assert `isVisible()`, not element state. `isChecked()` passes fine against a closed panel,
   which is how the V6 multiselect-closes-on-second-toggle bug reached Ignat instead of CI.

## Open, waiting on Ignat

- **Filtering Options** — presets/segments commit on click rather than staging (deliberate, flagged);
  segment counts need a status-count aggregate on the list endpoint; 5 segments collapse 8 statuses
  but production has ~18 → grouping is a product decision; two reset labels should become one
- **Settings & Billing** — pick a layout (A / B / C, or April's hybrid); SPEC-1 add-on rates
  (all prototype prices are still placeholders; the $0.04/extra-sync + $500 cap lead is unconfirmed);
  SPEC-2 Member-role contradiction; SPEC-5 dunning schedule. Validator pass on `organization.html`
  still not run — Ignat asked for it "later"
- **Reports / Financials Overview** — write the corrected 16 template descriptions in the agreed
  voice; decide the Jira split (one story vs. restructure + content-fix). Draft ready, ticket not
  created (Jira is read-only for me). Slack summary drafted, not sent
- **Balance Reconciliation (DIS-259)** — ~6 copy/tooltip items awaiting confirmation; Error-status
  "See details" wiring unverified; CONFIGURE screen copy not locked
- **ETC Notifications** — v1 published, no feedback yet; 4 open questions (system-health alarms in
  the dispatcher bell? filtering at 100/day with no expiry? actionable vs read-only? sheet language
  HEB/ENG?)
- **Smart Rules (DIS-336)**, **PIMS Grunddaten Editor**, **ETC website** — all built, all awaiting feedback

## Git

Working tree has uncommitted automation churn only: `.clawmetry-*`, `DREAMS.md`, `MEMORY.md`,
`memory/.dreams/*`, `memory/morning-ideas.json`, plus the three new dreaming files.
No source or prototype changes pending.
