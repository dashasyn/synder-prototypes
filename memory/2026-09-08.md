
## Juke box — back to one source per event (Ignat, 09:48)

"We need only one source per event." So yesterday's multi-source model is reverted and the data is
flat again: `event.source = { kind, refId, days[] }` — no one-item array left behind, no
add/remove-source buttons, no "Quelle 1" heading.

His own morning/evening example now lives as **two events** (`Klassik-Radio Vormittag`,
`Jazz am Abend`, same five stations, non-overlapping windows). That is arguably better than what I
built yesterday: an event has exactly one rank, so the priority rule (single track > playlist >
radio) stays readable — with several sources per event the rank was ambiguous the moment someone
mixed kinds.

The weekly grid, the period with the open end, the status derivation and the equal-rank overlap
warning are all unchanged. Validation messages lost their "Quelle N" prefix.

124 checks, all passing. Two rounds in two days on the same field is a signal to keep the model flat
until he has talked to the team.
