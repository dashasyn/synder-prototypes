# 2026-09-01

## ETC/BVG — new prototype: ELA-Meldungsgenerator (AI announcement copy)

Ignat asked for a prototype of the audio message generator for BVG disruption messages, and
explicitly asked for a critique of his sketch. He sent a screenshot of the dev's "Meldung
bearbeiten" panel (Ereignis U2 / Kein Halt / Notarzteinsatz on the left, DAISY + ELA + Tonfall +
GENERATE / GENERATE AUDIO on the right).

**Built:** `projects/etc-message-generator/index.html` →
https://dashasyn.github.io/synder-prototypes/projects/etc-message-generator/
Material Design 2, German UI, reuses the element set from `projects/etc-optic-timing`.
Verified with `projects/etc-message-generator/verify.js` — 46 Chromium checks, all passing.
Screenshots: /tmp/mg-1-initial.png … /tmp/mg-8-pron.png. Added to the prototype hub.

Full design decisions and the open questions are in `PROJECTS.md` under
"PIMS · ELA-Meldungsgenerator". Headlines:
- Quelle = Standardtext / KI-generiert / Manuell, so a dead text service doesn't block a live
  disruption message.
- Prompt assembled from structured facts; alternative transport is a select constrained to the
  line network so the model can't invent a bus line. Expert mode can decouple the prompt.
- Tone gated by event category — Locker/Humorvoll locked for Störung/Personenschaden.
- Tonalität (wording) separated from Stimme (TTS voice per language).
- Faktenabgleich against DAISY; audio state per language, invalidated by text/voice/pronunciation.
- Freigabe gate: both texts + current audio + actually listened to + fact check clean.

## Lesson: blur re-render swallows the next click

Found in real Chromium, not jsdom. Editing the DE text then clicking "Audio erzeugen" did nothing
on the first click: `onblur` fired on the button's mousedown, ran a full `render()`, replaced the
button node, and `mouseup` landed on a fresh element so no click event fired. Playwright's
`.click()` doesn't fail on this — it clicks happily and the handler never runs, so the only way it
surfaced was asserting the *resulting state* ("DE audio ready") rather than the click itself.

Fix: `commitEdit()` only records the edit. Everything text-dependent updates in `oninput` through
surgical DOM updates (`renderMsgMeta`) that never replace the textarea.

Two rules for next time:
1. Never call a full re-render from a `blur` handler.
2. After a UI action in a test, assert the state the action was supposed to produce — never assume
   the click landed because the click call returned.
