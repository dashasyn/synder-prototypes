
## Weekly LogRocket friction report (Aug 24–31, 2026) — cron run 07:00 UTC

**API lesson:** Galileo rejects custom `chatID` strings with a 500 (`ask-galileo-chats/<id>/` INTERNAL_SERVER_ERROR).
Must POST with NO chatID, let it generate a UUID, then follow up in that UUID. The cron brief's
`weekly-friction-<YYYYMMDD>` chatID format is broken — 3 retries all 500'd, a no-chatID probe worked instantly.
Also: hard rate limit, ~5 POSTs then 429 with "Expected available in N seconds". Space calls ~60s+.
GET on the chat endpoint returns 403 — retrieval only via follow-up POST.

**Numbers:**
- Rage clicks: 99 sessions (go.synder.com, Aug 24–31). Top pages /transaction/list, /ui/transactionReconciliation,
  /organizations/settings. Page counts are daily-bucket volume, not unique sessions. Element names NOT resolved.
- Dead clicks: 160 sessions (179 prior week, −10.6%). Same top pages. Element table = 2,236 buckets / 158 sessions.
- Funnels (7d, correctly scoped this time): onboarding 11→1→0; import 1044→128→3 (0.29%);
  reconciliation 954→67→18 (1.89%).
- Total app sessions Aug 24–31: 3,672. No prior-week baseline (relative-range bug).
- Errors: BROKEN, not zero. Both windows collapsed to Aug 24–31, severity default (severe-only) never overridden.

**Gaps carried forward:** element names never resolve at app-wide scope — next week scope the element
metric to a single page (/transaction/list) so selectors surface. Error instrumentation needs a manual
sanity check in the LogRocket Errors tab with no date/severity filter.

## ETC Optic / PIS timing — MD2 restyle (14:15–15:20 UTC)

Ignat: "The over all logic is okay. But I want to update the style. Use material design 2 elements.
Remove starting screen. Show the interface immediately. The add optic feature should be full screen
instead of side sheet." + two reference screenshots from the real ISR product (Device list, Device
details).

Done, same URL:
1. **MD2 rebuild.** Roboto, 48px app bar (notification badge + user, as in his screenshot), permanent
   nav drawer, elevation cards instead of 1px borders, filled text fields (grey fill, floating 12px
   label, 1px baseline → 2px primary on focus, dotted baseline when disabled), uppercase
   contained/outlined/text buttons at 36px, icon buttons, pill chips, data-table footer with
   rows-per-page + pagination, bottom-left snackbar. Tokens in `:root`.
2. **Cover screen deleted.** List renders on load. Presenter bar kept.
3. **Optic detail/add is now a full page**, modelled on his Device details shot: breadcrumb
   `Optics › Optic details`, title, COPY OPTIC URL + SAVE top right, cards Identity / Location in the
   network / Properties / Triggers. New optic gets editable station/platform/track (nothing imported
   yet) + blank/duplicate ID validation; existing optic locks the ID. Delete refused while a timing
   row uses the optic. Timing editors stayed side sheets — he only asked about add optic.

Made the table footer real rather than decorative (page slicing, disabled first/prev/next/last,
range text) — a dead pagination control is exactly the AUTO- finding class from the transactions
review.

71 DOM assertions in real Chromium, 0 JS errors, no horizontal overflow.
`scripts/verify-optic-md2.cjs` + `scripts/shots-optic-md2.cjs` (6 screenshots refreshed).

Nothing about the five open ticket questions changed — type 50, offset anchor, override granularity,
optic fallback, and the missing track schematic are all still open.
