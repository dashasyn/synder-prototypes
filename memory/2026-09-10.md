
## Reconciliation setup page — Ignat picked B; rebuilt against the live app

Ignat: *"I like option B. Remove other variants"* + four gaps. A and C deleted; `projects/recon-setup/index.html` is now B.

**Live app access came back** — `.synder-state/storage-state.json` had renewed CF_Authorization cookies (valid to Oct 3). Worth remembering: `demo.synderapp.com` returns **200 even when locked out** (the Cloudflare Access login page is a 200), so HTTP status is useless as an access check — load it with the stored state and read the page title instead.

**v11.7.79 explored. Several of my recommendations have shipped:** "Automation mode" → "Import method" (F9), CTA → "Run reconciliation" (F10), left column "Synder" → "Accounting", "Clearing account" → "Account to reconcile", Get-started step 3 updated. **Date range and the accounting import method are now prefilled** — so my Sep 9 line "six required questions, prefills none" was stale; only Account to reconcile is empty. Still unfixed: the "Reconciliation details" title, footer bleed, Get-started panel, and the account list is still **not** scoped to clearing accounts (11 options incl. `Stripe fees`, `Cost of Goods Sold`).

**Measured import-method matrix** (this settles the contradiction I flagged twice):
- Accounting side: **2** methods — Automated / Manual
- Integration side: **3** — Automated (Recommended) / Assisted / Manual
- Assisted = **2 named files**, each with real 5-step instructions; Manual = **1 file, no guidance**
- Worst case **3 upload areas** on one screen (books Manual + integration Assisted)

So my *original* note was right and my Sep 9 "correction" was wrong. Lesson: don't "correct" a recorded measurement from a screenshot whose column ownership I inferred — re-measure or say I can't tell.

**Verified:** 73 assertions, 0 page errors. Three defects caught in build: collapsing the Data section left no way to opt into uploading; the account hint claimed "the clearing account" even for `Stripe fees` (the *same* expiring-hint class I wrote into DESIGN_RULES.md yesterday — it recurred within a day, on a different field); disabled Run had no explanation, which DESIGN_RULES.md forbids.

**Placeholders to flag if presented:** only Stripe is connected in the demo org, so PayPal/Shopify capability and instructions are invented. Stripe copy is verbatim production.

### Follow-up same day: accordion + example-marked copy

Ignat: *"Descriptions for Stripe Assisted as an example. How to get files look like a link, it should look like accordeon"*

- **"How to get this file" is now an accordion**, not an underlined link: full-width bordered header, chevron that rotates, `aria-expanded`, body attached to the header. Open state is held in a `open{}` map because `render()` rebuilds the Data section — otherwise expanding then touching any field would silently collapse it.
- **Only Stripe Assisted copy is real.** Marked every other instruction block `example: true`, which renders an italic note in the panel itself: *"Example steps — the real report path for this integration still needs confirming."* Previously the placeholder warning existed only as a code comment and in my chat message, which meant the page looked authoritative to anyone who opened it without reading either. Lesson worth keeping: **if a prototype contains invented domain copy, the page has to say so — a caveat in the handover message doesn't travel with the file.**
- 86 assertions, 0 page errors.

### Ignat sent the production Assisted/Manual screenshot — matched its structure

His "as an example" meant *this is what it looks like in production*. Four differences from what I'd built, all now matched:
1. Each required file sits in **its own bordered card labelled "Upload file *"** — generic label, not the file name.
2. The **file identity lives in the accordion header**: *"How to get Balance change from the activity from Stripe"* (production wording, kept verbatim via a `how:` field).
3. **Chevron is on the left**, before the label — not right-aligned.
4. Accordions are **expanded by default**; the panel is a grey-5 block with no separate header border.
Dropzone also matched: cloud icon, "**Browse** or drag your file here", "CSV or XLSX formats, up to 100MB".

92 assertions, 0 page errors.

**Finding worth raising from doing this:** because production labels every field "Upload file", collapsing the accordions leaves 2–3 identical dropzones with nothing saying which file belongs in which — the identity is only inside the accordion header. Didn't change it (matching production was the ask), flagged it instead.

**Process note:** "X as an example" from Ignat can mean *here is the reference to copy*, not *use X's content*. I guessed the latter first and spent a step marking placeholder copy — useful anyway, but the screenshot was the actual answer. When a message is that terse and an image follows, wait for the image.

**Why production's accordion is open by default (Ignat, 16:00):** because the field label is the generic "Upload file", the expanded accordion header is what identifies which report goes in which dropzone. So the accordion isn't decoration — it carries the field's identity. Consequence: collapsing it is lossy by design, which is an argument for either never offering collapse or moving the name into the label. My prototype already defaults open, so it matches; no change made.
