# 2026-08-22

## Snapshot (23:00 Vilnius / 20:00 UTC, scheduled end-of-day review)

**No work session today.** No commits, no prototype edits, no messages from Ignat, no reviews run.
Yesterday (08-21) was the opposite — 17 commits between 13:57 and 18:18 UTC on the filters /
transactions-list thread. Everything that touched the workspace today was background automation:

- `03:00` — dreaming pass wrote `memory/dreaming/{light,deep,rem}/2026-08-22.md`, appended to
  `DREAMS.md` and `memory/.dreams/events.jsonl`, and rotated the 08-20 and 08-21 session corpora.
- `03:00` — MEMORY.md promotion block rotated. **Drift recurrence worth noting:** the block now
  promotes six entries, and all six are *snapshot meta-notes from the 08-16 quiet-day log*
  ("No work session today — fifth quiet day in a row", the promotion-block self-correction, etc.)
  rather than product findings. This is the failure mode observed in mid-August: after a run of
  quiet days the recency-weighted scorer has nothing real to rank, so it promotes its own
  bookkeeping. Yesterday's substantive log (17 commits, four defects of one CSS-port class) was
  **not** promoted. Not fixing it — but if it repeats after a busy day, the scorer is genuinely
  mis-weighting, not just starved.
- `07:08` — morning-ideas pass wrote entry 89 for 2026-08-22: an accessibility edge case created by
  yesterday's own V6 control work — **"Clear filters" removes itself the instant it's used**, so a
  keyboard user's focus falls to `document.body` and the next Tab restarts at the top of the page;
  a screen reader hears the control vanish with nothing announced in its place. Suggested fix is
  two lines and adds no UI: on clear, move focus to the filter bar / first filter control, or keep
  the button mounted-and-disabled instead of hidden (which also stops the layout shifting as it
  appears and disappears). Same self-removing shape exists on the V7 side sheet's close action —
  check both together.

## Carried forward, unchanged (nothing closed today)

**Filters / transactions-list thread** (live: https://dashasyn.github.io/synder-prototypes/filtering-options/)
- Reset label naming: "Clear filters" vs "Clear all" — pick one.
- V6 asymmetry: segments commit instantly while status needs Apply (flagged as my own call, not Ignat's).
- V5 presets commit-on-click vs staging.
- 8-group vs ~19-status taxonomy — product decision; also gates the pills-vs-dropdown fork on the
  earlier "variant 8" tab/group prototype.
- Whether to delete the drifted `reports/filtering-options/index.html` mirror.
- New from today's morning-ideas: the Clear-filters focus-loss a11y fix above.

**Onboarding connect step (SET-493)** — canonical copy in `onboarding/connect-step-copy.md`
- Still unresolved after two asks (10 Aug, 21 Aug): does Synder create the Stripe payment at
  invoice **send** time or at customer **pay** time? Copy currently assumes pay time; if it's send
  time, one "why" bullet and one safety bullet both need rewording. Needs Ignat/product.
- Six screen fixes flagged 08-21 and not yet fixed: colon inconsistency across the three screens,
  "XCart" tab vs "X-Cart" field labels, missing `?` tooltip trigger on the QBO first bullet,
  missing Synder logo in the QBO sidebar, QBO tooltip anchoring to the QuickBooks logo instead of
  its bullet, and the awkward "…don't exist yet" wrap stranding "yet" + icon on its own line.

**Settings & Billing rework** — 3 published layouts; add-on prices still placeholders and the
Member-role spec is still self-contradictory.

## Lesson reinforced (no new one today)
Yesterday's fourth CSS-port defect and fourth bad-assertion incident both stand as logged. Nothing
today tested either rule.
