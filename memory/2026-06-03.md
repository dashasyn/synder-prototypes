# 2026-06-03

## Manual Match Prototype V3

Built from scratch (well, copied V2 and surgically modified). Spec from Ignat:
- Combination of variants A+B from V2
- Keep two columns, Matched manually as separate tab
- Manual + AI + Manual JE all go to Matched manually with type badges + filter
- Make JE a button near Process log instead of separate tab — always 1 transaction (in V3 still supports bulk, just no separate tab)
- Process log button in top-right corner (already existed, was an alert stub) → wired to side sheet
- AI match popup with 1:1, 1:N, N:N examples → moved to side sheet for more space
- Floating Match button bottom, no calculations, just CTA

**Built:**
- Removed variant switcher entirely (single variant)
- Removed Journal entry tab
- Added Process log side sheet (480px) — shows JE cards + activity timeline
- Added Match type filter dropdown in Matched manually tab
- Match type badges: Manual (green), AI (purple), Journal entry (amber)
- AI Match side sheet (560px) with card-based layout showing pair kind (1:1, 1:N, N:N) + rule chip
- New aiMatchPairs include split payouts (s14 → i13+i14), bundled payments, refund+charge clusters
- Floating Match console: info banner ("Synder will check balance on click") + Cancel + Match
- Per-column toolbars cleaned up: synder=[Create JE, Ignore, Clear], integration=[Ignore, Clear]
- handlePostJE now pushes a manualMatches entry with matchType='je' + jeId reference
- confirmDeleteJE cleans up both journalEntries AND the corresponding manualMatches entry
- activityLog array tracks JE create/delete + AI batches for Process log timeline

**Background agent fiasco:**
- First attempt spawned a sonnet subagent that timed out after 180s — wrote no file
- Pivoted to building it myself via Edit tool. Took ~25 incremental edits but cleaner.
- Lesson: for prototype copy-and-modify work, just do it inline. Subagents are overhead.

**Live:** https://dashasyn.github.io/synder-prototypes/manual-match-v3/

Ignat to compare V3 vs V2 and decide which to ship.

---

## End-of-Day Snapshot (23:00 Vilnius)

**Done today:**
- Manual Match Prototype V3 — built and published
- URL: https://dashasyn.github.io/synder-prototypes/manual-match-v3/
- All spec items from Ignat implemented (Process log side sheet, AI Match side sheet, match type badges, floating Match CTA, JE as button, filter in Matched manually tab)
- Added to prototype hub index

**Pending:**
- Ignat's decision: V2 vs V3 — which variant to ship

**Status:** Clean. No blockers.
