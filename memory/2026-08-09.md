# 2026-08-09 (Sunday)

## Session Summary

**No work session today.** No messages from Ignat, no design work, no prototype edits, no commits.
Second consecutive quiet day (2026-08-08 was also empty).

Only automated background activity touched the workspace:

- `03:00` — dreaming pass (light/rem/deep logs for 2026-08-09, `DREAMS.md`)
- `07:0x` — `memory/morning-ideas.json` daily idea entry
- `~17:00` — clawmetry fleet/metrics files

Last real working day: **2026-08-07** (Synder demo access recipe, 8-round suggested-mapping banner
review, Filtering Options v7). See `memory/2026-08-07.md`. The backlog below is unchanged from
yesterday's snapshot — nothing was resolved because nothing was worked.

## Today's morning idea (worth acting on)

**Mapping count badge in nav, not banner** — the banner's dismiss/defer control has been a copy
placeholder ("Later") for four rounds with no UI. Linear/GitHub/Slack all signal recurring pending
work with a count badge on the nav item rather than a repeating in-page banner, because a banner for
recurring work needs a dismiss control, and once dismissed it can't report when the count changes.
R4 established mapping *is* recurring. Putting the count on the sidebar Mapping item does the
notifying and lets the banner shrink to a plain instruction line with no dismiss logic at all. The
number already exists — the dashboard reads "8 — mappings need configuration."
→ Next step if actioned: mock the sidebar Mapping item with a count badge beside the current banner
concept, then check whether the banner still needs to carry the count.

## Snapshot: open items carried forward

### Suggested-mapping banner (highest activity, zero sign-off — 4 days stale)
- 6 banner copy variants + top pick (#1, reassurance-first) awaiting Ignat's choice
- Zero-attention-required banner state — drafted, never reviewed
- Preview header wording ("We've mapped 7 of 8 groups for you" vs. alternatives)
- Section label for *suggested* rows (to pair with confirmed "Attention required")
- Account subtype column removal in R7 mockup — intentional? (asked twice, unanswered)
- "Save all" label when an Attention-required row is left empty
- Dismiss/defer ("Later") control — copy concept only, no UI (see morning idea above)
- Ordering heuristics (Synder-created-first / weak-match-first / group-by-subtype) — never mocked
- Eng check: is the unmapped count recomputed per screen open or cached from page load?
- **Unanswered since round 1:** does this banner apply to Summary Sync mapping only, or also the
  per-transaction mapping page?

### Filtering Options prototype
- **v7 still unverified in a real browser.** No jsdom or Playwright artifact exists. Every prior
  version was verified before being called done — v7 is not done until it's driven in Chromium and
  screenshotted.
- V5/V6 staging decision, segment-count aggregate, 5-vs-18 status grouping, duplicate reset labels,
  "In your books" naming, Summary-lines copy choice, no-transactions empty-detail check,
  pending-transactions auto-inclusion — open since 2026-08-06

### Other projects (untouched)
- **Financials Overview** — 16 template descriptions rewrite. Flagged as "next actionable step" on
  2026-08-06, -07 and -08. **Four days carried now.** Purely mine to execute; no decision needed.
- **Balance Reconciliation (DIS-259)** — ~7 open copy/tooltip decisions awaiting Ignat
- **Settings & Billing Rework** — 3 prototypes live, awaiting Ignat's pick; add-on prices are
  placeholders, Member-role spec is contradictory
- **ETC Notifications** — v1 published, awaiting feedback; 4 open questions
- **Replit `Summaryfiltering` repl** — still no access; asked 2026-08-06 whether it's a new build or
  the same March prototype. Unanswered.

## Pattern

Same read as yesterday, one day worse. The backlog is decision-bound, not work-bound — nine
mapping-banner items and all seven Balance Rec items need a call from Ignat. Two items are mine
alone and both slipped again over a quiet weekend: the Financials template descriptions (4 days) and
the v7 browser verification. Neither needs input. Start there Monday without waiting for a prompt.
