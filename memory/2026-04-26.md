# 2026-04-26

## Morning Idea — Quick-win
- **Topic:** Variant-aware `<title>` on the pricing prototype
- **Angle:** Quick-win (last used Apr 18 — 8 days ago, oldest in rotation)
- **Idea:** Currently every pricing-prototype variant tab shows the same `Synder Pricing - Choose Your Plan`. With 5 variants behind one URL via the JS switcher, reviewers can't:
  - Tell open tabs apart
  - Bookmark a specific variant
  - Paste a deep link in Telegram saying *"I prefer this one →"* without re-explaining which one
  Day 11 of pricing silence may partly be: nobody can refer to a specific variant unambiguously.

  Fix is ~3 lines hooked into the existing `data-version` switch handler:
  ```
  document.title = `Synder Pricing — V${n} (${variantLabel})`;
  ```
  Pairs naturally with the Apr 22 `?clean=1` idea — each clean-URL variant gets its own unique tab title automatically.
- **Built on:**
  - Day 11 of pricing review silence (stuck since Apr 16) — bundled pitch (5 stacked low-effort wins) still uncollected
  - Verified before pitching by grepping `<title>` across all live prototypes: pricing prototype's is static; RevRec / recon / audits-hub already have decent titles
  - **Different lane from the 5 stacked queue items** — this is reviewer ergonomics / metadata, not another card design. Doesn't pile a 6th visible-UI tweak onto pricing
  - Echoes Apr 21–24 pattern of cross-cutting low-cost wins that sharpen review-loop friction rather than designing more
- **Sent:** Telegram direct, message 541
