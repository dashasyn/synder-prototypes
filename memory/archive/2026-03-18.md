# 2026-03-18 — Daily Log

## Synder Login
- Re-logged into Synder via Cloudflare Access (code from Ignat: 410366) + Synder credentials
- Created backup of storage-state.json to prevent accidental cookie overwrites
- **Lesson:** Never save browser state after a failed login. Only save after confirming URL contains `controlPanel`. Also backup state before risky operations.

## Error Copy Audit — Settings (Per-Transaction Mode)
- Ignat asked to enrich the existing `empty-states-audit-2026-03-07.md` report with error copy from Settings page
- Scope: Only Settings page in per-transaction mode, NOT entire Synder UI
- Systematically toggled all toggles across 7 tabs (General, Sales, Invoices, Products/Services, Taxes, Expenses, Payouts)

### Error Messages Found (9 total):
1. **"Update unavailable: wait until your settings page is loaded and try again."** — Critical blocker, popover on every toggle click. 29 hidden spinners in DOM never resolve. Blocks ALL saves.
2. **"Turn on auto-sync"** — Cryptic label when Auto-import is OFF. No explanation of dependency.
3. **"Upgrade to use"** — Apply location toggle (General tab). Vague upsell, no plan name.
4. **"This feature is available on higher plans. Upgrade plan"** — Product mapping tab (entire tab locked). Different wording from #3.
5. **"This feature is available on higher plans. Upgrade plan"** — Sync discounts as products toggle (Sales tab). Same as #4.
6. **"You should turn off custom transaction numbers in QuickBooks company. Please, go to Company Settings -> Sales -> Custom transaction numbers to disable this setting."** — Enable QuickBooks Doc Numbers toggle (Sales). Harsh tone, no link.
7. **QBO Classes warning** (hidden in DOM) — "Warning: The classes feature is enabled in QuickBooks, however, there are no actual classes found..."
8. **Apply Taxes helper text** — Changes between ON/OFF states (unique behavior, only toggle that does this)
9. **Process payouts** — Uses Yes/No labels instead of On/Off. Also "app" instead of "Synder".

### Copy Consistency Issues (5):
1. "App" vs "Synder" — Fees/Payouts say "App", everywhere else says "Synder"
2. Toggle labels: On/Off vs Yes/No (Payouts)
3. Two different upsell messages for same concept
4. Inconsistent "Learn more >>" links
5. Helper text voice alternates between 3 patterns

## Reports & Tools Created
- Updated `reports/per-transaction-audit.html` with new **⚠️ Error Copy** tab (9 errors with Option A/B rewrites + editable Approved columns)
- Created `reports/error-copy-audit.html` — standalone error catalog (separate file)
- Created `reports/copy-generator.html` — interactive tool for generating consistent settings copy. Has: Generator form, Copy Rules reference, AI Prompt for LLMs, History with export.
- Created `reports/index.html` — **Synder UX Tools** dashboard/hub landing page

## UX Tools Server Setup
- Built `reports/server.js` — pure Node.js HTTP server (no Express dependency issues) serving static files + JSON API for saving/loading approved copy
- Approved copy saves to `reports/data/{reportId}.json` on disk
- per-transaction-audit.html updated: auto-saves approved copy to server (1.5s debounce), Share URL button, Export Markdown
- Hosted via Cloudflare quick tunnel on port 8080
- **Current URL:** https://employee-col-spotlight-contractor.trycloudflare.com (temporary — changes on tunnel restart)
- Server process: `setsid node server.js` (PID in `ss -tlnp | grep 8080`)
- Tunnel process: `setsid /tmp/cloudflared tunnel --url http://127.0.0.1:8080`
- **Ignat wants:** Permanent web page with all UX tools. Need to either set up named CF tunnel or open firewall port (needs sudo).

## Key Files
- `/home/ubuntu/.openclaw/workspace/reports/per-transaction-audit.html` — main audit report
- `/home/ubuntu/.openclaw/workspace/reports/copy-generator.html` — copy generator tool
- `/home/ubuntu/.openclaw/workspace/reports/index.html` — tools hub
- `/home/ubuntu/.openclaw/workspace/reports/server.js` — server (pure Node.js, no deps)
- `/home/ubuntu/.openclaw/workspace/reports/start.sh` — startup script
- `/home/ubuntu/.openclaw/workspace/.synder-state/error-audit/` — screenshots + JSON data from toggle testing

## Server & Tunnel Stability (16:30 UTC)
- **Key lesson:** `exec` tool kills background `&` processes when shell session closes. Must use `exec` with `background: true` for persistent processes.
- Working pattern: `exec` with `background: true` + `exec node server.js` (no `&`, no `nohup`, no `setsid`)
- Server session ID: `lucky-fjord` (node server.js on :8080)
- Tunnel session ID: `lucky-haven` (cloudflared tunnel)
- **Current live URL:** https://trance-structure-magnet-beaches.trycloudflare.com
- Rewrote `reports/server.js` as pure Node.js HTTP (no Express) — fixes `MODULE_NOT_FOUND` errors when express was installed locally in reports/ but not found from other CWDs
- Dashboard landing page at `reports/index.html` — shows Active Tools, Reports, Coming Soon sections with status bar
- Ignat confirmed he wants a permanent web page with tools — still using quick tunnel (URL changes on restart)
