# 2026-08-17 (Monday)

## Weekly LogRocket friction report (cron, 07:00 UTC) — sent to Ignat on Telegram

### Galileo API notes (confirming/extending last week's lessons)
- **chatID must be server-generated** — confirmed again. Sending `weekly-friction-20260817-a` → 500. The cron prompt still instructs a custom chatID; POST with **no chatID**, read the returned UUIDv7, follow up in it.
- **Rate limit:** hit throttling after ~5–6 requests ("Expected available in 64 seconds"). Spacing follow-ups ~150s apart worked for 2 of 3; the third needed a retry after ~100s.
- **Follow-up prompt that works:** "Summarize your findings as plain text with concrete numbers. No charts, no visualizations, no follow-up questions."
- Total wall-clock for 3 queries: ~13 min.

### Data (Aug 11–17, 2026; Aug 17 partial)
- **Dead clicks:** 629 across 141 URLs. `/transaction/list` 87 (13.8%), `*/index` 82, `/sync/show/*` 57, `/controlPanel/index/overview` 50, `/organizations/settings` 40, `/company/settings` 36, `/company/settings/*` 34, `/organizations/view/*` 31, `/ui/transactionReconciliation` 26. Settings cluster = 110 (17%), shared template.
- **Rage clicks: NOT MEASURED.** Galileo built the metric with a frozen-frame filter (frames stalled >3s) instead of a rage-click filter and returned 0. Galileo itself flagged this. Last week the same question returned 657 sessions — so 0 is a broken query, not an absence. Reported as a gap, not as good news.
- **Funnels (90d — Galileo ignored the 7d ask again, 2nd week running):**
  - Onboarding: 149 → Connect 134 (−10.1%) → Sync/Import 75 (−44.0%). 50.3% e2e.
  - Import: 13,865 → Import historical 2,294 (−83.5%) → Import to Synder 1,733 (−24.5%) → Sync 1,094 (−36.9%). 7.89% e2e. Step 1 is generic navigation so −83.5% overstates; the real signal is **639 sessions that imported but never synced**.
  - Reconciliation: 2,358 → 0 → 0 → 0. Broken funnel — step 1 matched anonymous marketing-site traffic, and the flow spans two URL families (`go.synder.com/ui/transactionReconciliation/*` and `go.synder.com/accounting/public/reconciliation/index.html`). Same "which surface is real" ambiguity as last week.
- **Errors:** zero severe untriaged issues Aug 10–17. Same untriaged-only scope caveat as last week — still not a positive confirmation.
- **Volume:** 6,058 sessions (Aug 11–17) vs 7,849 (Aug 3–9) = −23%. Inflated by Aug 17 being partial (143 so far). Weekday-only softening ~10–15% (1,068–1,361 vs 1,193–1,546). Weekend cliff repeats (−55% after Aug 14) — normal.

### Finding of the week
**Sync Issues page traffic up 23% (74 → 91 sessions) while total volume fell 23%** — the only metric moving against the trend. Per-session rate 0.9% → 1.5%. Repeat accounts in the recent set. Candidate signal for backend sync degradation.

### Open / carried forward
- Two metrics need rebuilding before next week's run: **element-level rage clicks** and the **reconciliation funnel** (needs the correct entry step + correct URL family). Asked Ignat whether to fix these — awaiting answer.
- Element-level dead-click attribution still unresolved — 2nd week. Page-level counts are the only trustworthy layer.
- The cron prompt itself should be updated to drop the custom-chatID instruction, since it 500s every time.

## Snapshot (23:00 Vilnius / 20:00 UTC, scheduled end-of-day review)

**One piece of real work today: the weekly friction report** (detail above). Sixth day with no
messages from Ignat, no prototype edits, no commits. Everything else was background automation:
`03:00` dreaming pass + MEMORY.md promotion rotation, `07:0x` morning idea, `~17:00` clawmetry.
Last commit remains `4d5cfcd`; working tree has automation churn only.

Morning idea (2026-08-17) — **Reconciliation: show the artifact, not the explainer.** From the
08-10 figure that rotated into MEMORY.md: ~2/3 of 90 watched sessions read the "Match
transactions to your books" overview/FAQ/video and never clicked Run audit. Reading that as a
value-visibility failure rather than a comprehension one: replace the video/FAQ block with a
static sample audit result ("3 payouts not matched to deposits", "1 fee posted twice"),
captioned "This is what your audit will look like." No dev sprint, and the same Galileo query
a week later verifies it.

Keeping this short per yesterday's note — today's `light/2026-08-17.md` candidates are again all
sentences lifted from yesterday's snapshot prose, so long end-of-day writing on quiet days just
feeds itself.

## Open, waiting on Ignat (carried forward from 2026-08-16, plus today's two)

- **New today — LogRocket metrics:** rebuild element-level **rage clicks** (frozen-frame filter
  is wrong) and the **reconciliation funnel** (wrong entry step + two URL families). Asked, no
  answer yet. Both block next Monday's run being trustworthy.
- **Filtering Options** — presets/segments commit on click (deliberate, flagged); segment counts
  need a status-count aggregate on the list endpoint; 5 segments vs ~18 production statuses →
  product decision; two reset labels → one ("Clear filters" / "Clear all" proposal from 08-15)
- **Settings & Billing** — pick a layout (A/B/C or April's hybrid); SPEC-1 add-on rates (all
  prices placeholders), SPEC-2 Member-role contradiction, SPEC-5 dunning schedule; validator
  pass on `organization.html` still not run
- **Reports / Financials Overview** — write the corrected 16 template descriptions; decide the
  Jira split. Draft ready, ticket not created (Jira read-only). Slack summary drafted, not sent
- **Balance Reconciliation (DIS-259)** — ~6 copy/tooltip items; Error-status "See details"
  wiring unverified; CONFIGURE screen copy not locked
- **ETC Notifications** — v1 published, no feedback; 4 open questions (one reframed 08-16 as a
  lifespan problem: Active/All segmented control instead of filters)
- **Replit `Summaryfiltering`** — no access; waiting on a public preview URL or HTML/JS export
- **Smart Rules (DIS-336)**, **PIMS Grunddaten Editor**, **ETC website** — built, awaiting feedback
