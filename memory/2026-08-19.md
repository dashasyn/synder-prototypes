# 2026-08-19

## Session resumed — ETC notifications follow-up + security finding (10:15–10:29 UTC, Telegram topic 7433)

Ignat opened asking for help on "a transportation product" with no further detail. I surfaced
the ETC notifications context (three open questions from 2026-08-05: system-health alerts in
dispatcher bell, filtering/collapsing at 100/day volume, actionable-vs-deletable notifications).

**Ignat's answer: those three are resolved — fixed inside the product already.** No further
detail given on how; not asked, since he indicated no questions remain. This closes out the
open items list carried in MEMORY.md/PROJECTS.md from the 2026-08-05 session.

He has not yet said what the new transportation-product ask actually is — still waiting on that.

### Security finding — surfaced this turn, unresolved
While trying to push registry updates, discovered **`.github-token` is tracked in git**,
containing a live `ghp_…` token. Committed by an earlier session in commit `b8ab132` and again
`d295108` (2026-08-11). GitHub push protection has been rejecting every push since — **21 local
commits are unpushed**, including several daily logs. Token has not reached GitHub (push
protection caught it before publish), so exposure is local-disk only, but it's real.

I did **not** click GitHub's "allow this secret" bypass — doing so would publish a working token
to a repo whose Pages site is public. Flagged two remediation steps to Ignat, neither done yet
pending his go-ahead:
1. **Rotate the token** — safe regardless of whether it leaked.
2. **Rewrite history** to remove the file from the two commits, untrack it, add `.github-token`,
   `.figma-token`, `.synder-creds`, `.atlassian-creds` to `.gitignore` (only the GitHub token
   tripped the scanner, but the others carry the same exposure risk). This needs a force-push,
   which I won't do without explicit confirmation since it rewrites shared history.

**Status: unresolved, awaiting Ignat's decision on rotate + history rewrite.** 21 commits still
sitting local-only until this is fixed — includes anything logged after 2026-08-11 that hasn't
synced to the GitHub Pages hub.

### Open TODO for next session
- Ask Ignat to confirm: rotate `.github-token` + rewrite history + gitignore the four credential
  files, OR handle differently (e.g. BFG repo-cleaner, or just start a fresh repo).
- Once unblocked, push the 21 pending commits.
- Get the actual ask for "the transportation product" help he opened this session with — never
  specified beyond the opening line.

## Berlin metro (PIMS/BVG) — maintenance dialog copy (10:29–10:56 UTC, same thread)

**New task identified**, separate from ETC notifications. Ignat clarified the "transportation
product" ask: **PIMS** (BVG Berlin U-Bahn line management). Context: ops sometimes need to
temporarily merge lines (e.g. replace U1 + U3 with U12). During that change the system is
locked for 5–7 minutes; users have to log out, wait, then log back in with username/password.
He showed a login-screen modal with a maintenance message and asked whether it should avoid
stating the specific reason (i.e. stay neutral, not say "we're merging U1/U3 into U12").

### My take, given
- **Agreed: stay neutral about the cause.** Line-merge specifics are internal and irrelevant to
  the user's task; naming it invites the wrong follow-up question.
- **But recommended adding "planned"** — "a planned update is running" — since silence + a dead
  system for 7 minutes can otherwise read as an outage to on-call BVG staff, prompting escalation.
- **Flagged the "5 min" vs actual "5–7 min" mismatch** — a stated wait time shorter than reality
  guarantees failed retries. Recommended either a padded estimate or (better) a fixed clock time
  ("available again from 10:45") instead of a countdown/duration.
- **Flagged the OKAY button as the wrong pattern** — dismissing it just returns the user to a
  login form that will still fail. Recommended either replacing the form with the message, or
  disabling the form and showing the message above it, with no acknowledgement button.
- **Bigger structural suggestion:** eliminate manual "logout and wait" entirely — have the client
  poll and auto-reload when the backend returns, and ideally show an advance-warning banner
  ~10 min before the update starts, so nobody gets caught mid-task.
- Provided German + English copy pairs using a fixed target time instead of a duration.
- **Two smaller findings in the screenshot itself:** the login form shows "Password" instead of
  "Passwort" next to German "Benutzer"/"ANMELDEN" (likely a copy slip); and the modal is in
  English while the form is German — worth deciding which language the UI actually targets.
- **Open question asked, not yet answered:** if the lock hits mid-edit, does unsaved work
  survive? If not, the advance-warning banner becomes a requirement, not a nice-to-have.

### Status
Awaiting Ignat's response on: the mid-edit data-loss question, and whether he wants any of this
(copy rewrite, disabled-form pattern, auto-reload/advance-warning) actually implemented or just
discussed as feedback for now.

### Repo status carried forward (unresolved from earlier this session)
`.github-token` still tracked in git with a live token; push protection blocking 21 local
commits. Awaiting Ignat's go-ahead on rotate + history rewrite + gitignore (see above, ~10:15
entry). Not yet actioned.
# 2026-08-19 — Daily Log

## Pre-compaction memory flush (11:24 UTC)

### Last real work: 2026-08-18, "Reconciliation details" overlay deep analysis
Ignat asked (19:44 UTC on 08-18) for a full analysis of the "New reconciliation" creation overlay: login to Synder demo, go to Transaction reconciliation, start creating a new reconciliation, deeply analyze the full-page overlay ("Reconciliation detail"), check states/dropdowns/selectors/wording, give quick wins + real improvements.

**Blocker hit:** Could not log into demo.synderapp.com. Cloudflare Access rejected the service token in `.synder-creds` — verified via curl, CF meta JWT came back `service_token_status: false` (token revoked/expired, not a header/config issue). No fallback path (Google SSO / email code) usable by me.

**What I did instead:** Worked from existing captures on disk (April 30, v11.6.66) covering 4 states of the overlay: empty, all-required validation, mode dropdown open, datepicker open. Delivered a full analysis anyway rather than blocking entirely.

**Three questions asked, still unanswered by Ignat as of this flush:**
1. Can Ignat get me a fresh CF Access service token, or should I work from screenshots he captures manually going forward?
2. Are the April 30 captures still representative, or has this overlay changed since (especially given Balance Reconciliation/DIS-259 work touching this area)?
3. Mode dropdown in the captures shows **Automated / Standard / Manual** — but all DIS-259/FDD work has been using **Automated / Assisted / Manual**. Which is current — did the FDD rename it, or is production stale/different?

**Full findings delivered (structural, wording, states/interaction, quick wins, real improvements) — see prior turn for full text.** Key points worth remembering long-term:
- Overlay is a full-page takeover for only 4 fields — mostly empty space, should be a modal
- Title says "Reconciliation details" even when creating new — should be "New reconciliation" in create context
- "Automation mode" field renamed to "Import method" elsewhere in our copy work but still shows old naming/options in this capture — inconsistency to track
- No date range presets (biggest quick win — reconciliation periods are almost always calendar months)
- Datepicker visually covers the Accounting column and its own error text
- Field order is backwards: Integration should come before Mode since available modes depend on integration (e.g. PayPal multi-currency can't do balance verification)
- No warning/awareness of overlapping reconciliation periods
- Mobile (375px): "Start matching" button disappears entirely from header, Automation mode select overflows — no usable primary action on phone
- Validation shows four identical generic "Required" messages with no per-field guidance
- Button label "Start matching" undersells what happens — locked In-progress tooltip says Synder is "gathering and processing," matching is just one phase

**Could not assess without live access:** Integration dropdown contents, what each mode reveals when selected (including Manual mode's balance entry fields from DIS-259), Standard vs Automated distinction, anything after clicking Start matching.

### Session state at this flush (11:24 UTC, 2026-08-19)
No reply yet from Ignat to the three questions above. Nothing new happened between the analysis delivery (08-18 ~19:44-20:xx UTC) and this flush ~15-16 hours later. Session idle overnight.

### Standing open items (unchanged, carried forward from 2026-08-04 / 2026-08-18)
- DOM-1 typo "blanced" → "balanced" — still not confirmed applied
- Beginning/Starting label-tooltip mismatch — fix proposed, not confirmed applied
- Difference/Delta figure has no direction indicator
- Difference tooltip could mirror Calculated-ending formula pattern (low priority)
- "USD" repeated on all six balance line-item figures
- Demo data inconsistencies (three different periods shown on balance block; identical rows in reconciliation list making sort unverifiable)
- Unverified: does "See details" on an Error row actually link to Process log
- Configure-screen "what is Balance reconciliation" copy rewrites (Manual checkbox tooltip + Automated "New" badge notice) — proposed since July 15, never confirmed/locked/applied

### Admin item — flagged again (8th time total across recent days)
**Balance Reconciliation (DIS-259) still has no PROJECTS.md entry.** Offered to add it explicitly on 2026-08-18 18:37 ("say the word and I will") — no go-ahead given yet. Do not add without explicit confirmation.

### New access issue to remember going forward
**Synder demo CF Access service token is broken as of 2026-08-18.** `service_token_status: false` per CF meta JWT. This blocks any future browser-automation task against demo.synderapp.com until Ignat issues a new token or provides another access path. Flag this proactively next time a task requires live Synder access, rather than rediscovering the same blocker.

## Update 12:04 UTC — second pre-compaction flush, same day

### New work since 11:24 entry: successfully logged into Synder, full live analysis delivered
Ignat said "Login to Synder again" at 11:24 UTC. This time login worked (previous CF Access service-token blocker from earlier in the day was resolved somehow — not explicitly discussed, but access succeeded). Logged into **Summary_test** org (only integration: Test account_1 / Stripe). App version now **v11.7.66** (was v11.6.66 in the April captures).

**Delivered a full live analysis of the "New reconciliation" creation overlay** — see prior turn for complete text. Key points to retain long-term:

**Confirmed FIXED since April captures:**
- Mode dropdown now only 2 options (Automated / Manual) with inline descriptions + "Recommended" badge — "Standard" option is gone entirely
- Date range now pre-fills to last full month by default
- Validation down to one field only (others have sane defaults)

**Verified NEW bugs found via live testing (not present in old static captures):**
1. **Typed dates bypass all validation** — tested `12/31/2002` to `12/31/2026` (24-year range, end date 4 months in future), no error shown. Calendar nav correctly blocks future months, but typing circumvents it entirely. Start-after-end also unchecked. Did NOT submit (avoided creating junk data in demo org) so server-side validation is unverified.
2. **Datepicker calendar desyncs from field value** — end field showed 12/31/2026 but calendar UI showed December 2020 (6 years off).
3. **Picking a start date silently overwrites/clears the end date** — picked Aug 12 as start, both fields became 08/12/2026 (1-day range), easy to miss.
4. **Overlay is not responsive** — scrollWidth stuck at 1280px at both 375px and 768px viewports. "Start matching" button at x=1117 is off-screen on both mobile and tablet. 85 overflowing elements on mobile, 47 on tablet — no usable primary action on phone or tablet.

**Structural findings (not bugs, but design gaps):**
5. Manual mode's file upload sits in the "Synder" column, but the uploaded file is the integration's report — wrong side of the Synder-vs-Integration comparison
6. Terminology drift: this screen says "Synder" for one side, results-page balance block says "Accounting" for the same side (e.g., "Accounting beginning balance... taken from your books")
7. "Platform: Summary_test (Synder)" shows org name, never the actual accounting company (QBO/Xero) — the one thing that field should communicate
8. Instructions panel says order is 1-date/2-integration/3-mode; actual form layout reads date→mode→integration (1-3-2 mismatch)
9. Integration should logically come before Mode since available modes/balance-verification capability depend on integration choice (ties to DIS-259 — PayPal multi-currency example)
10. "Any account" option contradicts the panel's own text: "Reconciliation is done one integration at a time"
11. Still a full-page overlay for only 3 fields — bottom third empty

**Copy issues:**
- Title "Reconciliation details" should be "New reconciliation" when creating
- Manual upload only says "CSV or XLSX, up to 100MB" — no guidance on WHICH report to upload, where to find/export it, or a template. Directly relevant: known real-world failure — August LogRocket data showed normalization failures on Stripe/Amazon file uploads due to unrecognized date formats. This copy gap is a live production pain point, not theoretical.
- "Start matching" button label undersells the process vs. our locked In-progress tooltip ("gathering and processing")
- Step-1 illustration shows "May 1 - May 31" but actual default range is July 1-31 — stale illustration
- No confirmation on X-close
- "Integration" duplicated as column heading AND field label

**Important note for DIS-259 (Balance Reconciliation):** confirmed **no balance verification exists in either mode in current production** — no checkbox, no balance input fields anywhere in the creation overlay. This means all the configure-screen copy drafted back in July (Manual-mode checkbox tooltip, Automated-mode "New" badge notice) still has nothing to attach to — feature hasn't shipped to this screen yet.

Quick wins and real improvements lists were also given (validate typed dates, stop start-date clearing end-date, rename title, add file guidance + template to Manual upload, fix stale illustration date, drop duplicate label / vs. make responsive, move upload to Integration column, reorder Integration→Mode, unify Synder/Accounting terminology, resolve "Any account", collapse to modal).

### Session state at this flush (12:04 UTC)
No reply yet from Ignat to the full analysis delivered ~11:30-ish UTC. This flush event is purely a marker — no new decisions, no new work.

### Access note — resolved, update from earlier entry
The CF Access service-token issue flagged at 11:24 UTC (`service_token_status: false`) is **no longer blocking** — login succeeded this session. Cause of the fix unknown/not discussed. If Synder access fails again in a future session, don't assume it's the same unresolved token — check fresh rather than citing the old blocker as still-active.

### Standing open items (unchanged, still carried forward)
- DOM-1 typo "blanced"→"balanced" — unconfirmed
- Beginning/Starting label-tooltip mismatch — unconfirmed
- Difference/Delta direction indicator — open
- Difference tooltip mirroring formula pattern — open, low priority
- "USD" repeated on all six balance figures — open
- Demo data inconsistencies (dates) — open
- "See details" → Process log link unverified
- Configure-screen balance-reconciliation copy — now confirmed to have NOTHING to attach to in production (see DIS-259 note above) — effectively still pending feature launch, not just copy approval

### Admin item — flagged again (9th time)
**Balance Reconciliation (DIS-259) still not in PROJECTS.md.** No go-ahead given yet despite repeated offers. Continue withholding until explicit confirmation.

## PIMS maintenance dialog — follow-up: retry button, night-time context (10:56–13:02 UTC, same thread)

Ignat added key context: the forced logout **happens at night, almost nobody working**, but
still needs to inform whoever is there. Asked: should the button be "Reload", or should he ask
devs to auto-reload the page?

### My revised recommendation
- **Dropped the auto-reload/polling suggestion** from earlier — that was based on assuming
  daytime users mid-task; not worth a dev ask to save a handful of night-shift people one click.
- **Use a retry button wired to a plain page reload** — "Erneut versuchen" / "Try again". Trivial
  to implement, no polling infra needed. If the maintenance window has passed, reload lands them
  on the login form; if not, they see the same dialog again — itself the answer.
- **Dialog should be non-dismissible** — no ✕, no backdrop click. Nothing useful behind it.
- **Dropped "OKAY"** — it falsely implies dismissing leads somewhere.
- **Absolute clock time matters even more at night** — someone arriving at 02:10 has no reference
  for when a relative "5 minutes" countdown started. Fixed time ("available again from 02:15")
  works regardless of arrival time.
- **Retracted the earlier "no need to log out" copy line** — backend forces logout, so that line
  would have been false. Final copy:

  DE: "Wartungsarbeiten — Ein geplantes Update läuft. Die Anmeldung ist ab **02:15** wieder
  möglich. [ Erneut versuchen ]"

  EN: "Scheduled maintenance — A planned update is running. You can sign in again from **02:15**.
  [ Try again ]"

- **Redirected the dev-effort question**: rather than spend it on auto-reload, better spent on
  whether anyone *already logged in* gets warned before being forcibly kicked, vs just landing
  on the login screen. Night shift is small but non-zero; someone mid-edit losing unsaved work
  from a silent kick is a worse failure than an extra click on a retry button.

### Status
Awaiting Ignat's decision on: final button/copy approval, and whether to pursue the
pre-kick-warning-banner idea with the dev team.

### Repo status — still unresolved, carried forward again
`.github-token` remains tracked in git with a live token; GitHub push protection still blocking
21 local commits (unchanged since ~10:15 and ~10:56 entries above). No action taken yet — still
awaiting Ignat's go-ahead on: (1) rotate the token, (2) approve a history rewrite/force-push to
strip the file from commits `b8ab132`/`d295108`, untrack it, and add `.github-token`,
`.figma-token`, `.synder-creds`, `.atlassian-creds` to `.gitignore`. This is now the single
oldest open item in today's log — flag it again next session if still silent.

## Update 13:06 UTC — third pre-compaction flush, same day

### New work since 12:04 entry: Ignat corrected several findings, I revised the analysis
Ignat pushed back hard on the quick-wins/real-improvements list from the 12:04 turn. Key corrections that MUST be remembered — do not re-flag these as issues in future reviews of this screen:

**Retracted findings (were wrong, based on incomplete form state):**
1. **Any date range is intentionally allowed** (even 20 years) — not a bug. Users can do whatever they want here.
2. **Datepicker behavior is correct as designed** — picking a new start date snapping the end date to match (avoiding past-date end) is intentional, not a desync bug. A resulting 1-day range is expected behavior, not silently wrong (though a "1 day selected" hint could still help).
3. **Illustration date should NOT dynamically match the selected range** — it's a generic illustration, expected to stay static across reconciliations.
4. **Product is desktop-only by design** — "Our product created only for Desktop." Do not flag responsive/mobile/tablet layout issues on this or similar screens going forward; it's out of scope entirely, not a bug.
5. **Both sides DO have file upload** — I was analyzing an incomplete form. Once integration AND account are selected, a second "Automation mode" selector appears on the Integration side, each side (Accounting / Integration) with its own mode + upload. My original "upload is on wrong side" finding was based on incomplete form state, not a real issue.
6. **Modes are already filtered by integration** — my suggestion to "reorder Integration before Mode so modes can be filtered" was redundant; filtering already happens.
7. **"Synder" vs "Accounting" terminology is intentional and dynamic, not drift** — Synder is used for Summary-type reconciliation, "Accounting" (with company name, e.g. "Accounting · Sandbox Company_US_1 (QuickBooks Online)") is used for per-transaction reconciliation. My original finding was based on testing in Summary_test org which has no accounting company connected, so I only saw the "Synder" label and wrongly called it inconsistent.
8. **Platform field DOES name the accounting company** when one is connected — my "uninformative" finding only applied to the no-accounting-company test org.
9. **Time expectation copy already exists** on the Integration side: "Automatic file retrieval can take some time — up to a few hours." My "no time expectation set" suggestion was already addressed, just not on the side I was looking at.
10. **Modal suggestion (real improvement #6) — Ignat didn't understand which modal I meant.** I clarified: meant replacing the full-page overlay with a smaller centered dialog. But withdrew it once the complete two-column form was revealed — the form now genuinely fills the space, so full-page/overlay format is justified, not oversized.

**New findings from completing the form (previously hidden by incomplete state):**
- Confirmed the second Automation mode selector + upload zone appears once integration + account are both chosen — this was the missing piece that invalidated several earlier findings
- **~180px dead vertical gap in the Accounting column** — Integration column fills equivalent space with Timezone + Currency helper fields; Accounting column has no equivalent content there, looks unfinished/like helpers were meant to be there
- **Site footer (About / Contact us / Legal / v11.7.66) renders INSIDE the overlay** below the form — in shorter viewports this clipped the required Upload field
- **Two fields both literally labeled "Automation mode"** with no distinguishing text — this IS the real version of Ignat's "we need a name for the selector" ask. Proposed options: (a) qualify each with context ("How to provide accounting data" / "How to provide integration data"), or (b) keep "Automation mode" as-is and rely on column headings, but then fix the step panel text "3. Choose automation mode" to be plural since there are now two
- **Upload guidance still genuinely missing** — dropzone only says "CSV or XLSX, up to 100MB" with no indication of WHICH report to export (Stripe: Payouts? Balance history? All transactions?). Directly tied to real production pain: August LogRocket data showed file normalization failures on Stripe/Amazon uploads from unrecognized date formats — users likely uploading wrong export type or wrong settings. This finding stands, not retracted.
- **"Any account" option in Integration dropdown genuinely unclear** — has a Synder hexagon icon, unknown function, and its existence contradicts the panel's own stated rule "Reconciliation is done one integration at a time." Screenshot saved: `.synder-state/recon-create-2026-08-19/04-integration-dropdown.png`. This finding stands, not retracted.
- **Date validation asymmetry noted but not fully resolved** — Ignat confirmed typed ranges are allowed by design, but I noted the *calendar UI* still blocks navigation past the current month while typing bypasses that entirely — inconsistency between the two input methods remains, Ignat's call on which one should change. Also flagged one possible testing artifact: typing `12/31/2026` in the start field produced `12/31/2002` — could be a mis-click on my part, recommended manual re-check rather than asserting a bug.

Captures from this full session saved to `.synder-state/recon-create-2026-08-19/` — 16 states total.

### Lesson learned — write this down for future screen reviews
**Complete the full form/flow before delivering a "deep analysis."** Several retracted findings above were artifacts of reviewing an incomplete form state (fields not yet filled, dependent fields not yet revealed) or of testing in an org (Summary_test) that lacks a connected accounting company, which hid dynamic/conditional UI and copy. Going forward: fill every field, trigger every conditional/dependent field, and if possible test in an org WITH a connected accounting company as well as one without, before writing up findings as bugs.

### Session state at this flush (13:06 UTC)
No reply yet from Ignat to the corrected/revised analysis delivered ~12:04-12:xx UTC. This is a pure continuity marker, no new work since.

### Standing items — status update
- The "Automation mode" naming ambiguity (point 6 above) is now the sharpest actionable item on this screen — needs Ignat's decision on naming approach
- "Any account" dropdown option and missing upload-format guidance remain genuinely open, unretracted findings
- DIS-259 configure-screen copy still has nothing to attach to in production (unchanged from 12:04 entry)
- PROJECTS.md registration for DIS-259 still not done — flagged repeatedly (10th time now across the last few days), no go-ahead given yet, continue withholding until explicit confirmation

## Update 13:49 UTC — fourth pre-compaction flush, same day

### New work since 13:06 entry: further correction (mode dropdown had 3 options, not 2), naming recommendation delivered
Ignat pushed back again at 13:06 on the previous round of findings — this time explicitly asking for "more careful, good investigation" (a direct quality note to remember). Corrections given:

**Retracted (again) — root cause of the error identified:**
- I had only ever opened the **Accounting-side** Automation mode dropdown (2 options: Automated/Manual). The **Integration side has 3 modes: Automated / Assisted / Manual** — meaning "Assisted" IS in production, matching the FDD exactly. My earlier claims that "Standard is gone" and "neither matches FDD's Assisted" were both wrong, caused by checking only one of the two independent selectors.
- **Step-by-step upload guidance DOES exist** — but only in Assisted mode, and only found once I opened the Integration-side dropdown properly. Two collapsible guidance blocks: "How to get Balance change from the activity from Stripe" and "How to get Payouts from Stripe," each with 5 numbered steps naming exact report, menu path, and export settings (e.g. "Itemized" with "All" columns). This directly satisfies the upload-guidance concern raised earlier — genuinely resolved, not a retraction of the concern itself but confirmation the feature already exists (just hidden from me due to my incomplete testing).

**Accepted from Ignat, no further action needed:**
- ~180px gap in Accounting column is intentional — deliberate symmetry with Integration column, "avoid chaos" was the design goal, not an oversight
- Form length acceptable — Manual mode form is long too, full-page overlay format is justified
- "Any account" clarified: lets user reconcile a GL account NOT tied to any integration (e.g. Checking, COGS, Inventory Asset, Stripe fees) — confirmed via account list, no longer a mystery/open question

**Still genuinely open (Manual mode gap, not retracted):**
- **Manual mode has zero guidance on either column** — just a bare upload dropzone, no report guidance (defensible since Manual = user owns the mapping). BUT: the Manual mode's own description text says "Upload your files and set up mapping rules yourself" and the upload box gives no path to actually create/access those mapping rules. Automated mode links to mapping rules; Manual — the mode that most needs them — doesn't. Flagged as the one clear actionable gap remaining. One-line fix: add a mapping-rules link/entry point to the Manual upload box.

**Automation mode naming — Ignat revealed this was already being decided internally**, between **"Import method"** and **"Statement source"**, and said he expected suggestions as part of this review (implicit feedback: should have asked/offered options earlier rather than waiting to be asked).

**My recommendation delivered: "Import method"** — reasoning:
- "Import" is already Synder's vocabulary-approved canonical verb for data coming in (vocabulary.md prefers it over "fetch/pull")
- Accurately describes all 3 options: Automated = Synder imports for you, Assisted = you supply files for Synder to import/normalize, Manual = you supply files + rules
- Works correctly on both columns despite asymmetric option counts (Accounting: 2 options, Integration: 3 options)

**Rejected "Statement source"** — two problems: (1) wrong axis — the actual source (Stripe or QBO) is identical across all 3 modes; what differs is *who does the retrieval/mapping work*, not the source itself; (2) "statement" isn't Synder terminology and is factually imprecise — the in-app guidance calls these "reports" (Balance summary, Payouts) and on Accounting side it's the GL register; introduces an unfamiliar noun for an already-named concept.

**Third option offered if full explicitness is wanted:** "How to provide data" — matches the Get-started panel's own existing step-3 wording verbatim, internally consistent, but longer as a field label.

**Follow-on note given:** whichever name is chosen, the Get-started panel's step 3 text should be updated to match (e.g. "3. Choose import method"), and worth clarifying in the panel that there's one selector per side (a second one only appears after account selection) since first-time users only see one initially.

### Lesson reinforced (second time now)
Same root cause as the 12:04 correction: **incomplete investigation before delivering findings.** This time specifically: only checking one of two independent dropdowns with the same label, assuming they had identical option sets without verifying both. Combined with the earlier lesson (fill every field, trigger every conditional, test with and without connected accounting company) — the pattern is: **when a screen has multiple instances of the same-labeled control, check each instance independently; don't assume parity.**

### Session state at this flush (13:49 UTC)
No reply yet from Ignat to the Import method / Statement source recommendation. Pure continuity marker, no new work since.

### Standing items — status update
- Naming decision (Import method vs Statement source vs "How to provide data") — awaiting Ignat's final call
- Manual mode missing mapping-rules link — genuinely open, one-line fix proposed
- DIS-259 configure-screen copy — still nothing to attach to in production (unchanged)
- PROJECTS.md registration for DIS-259 — still not done, flagged repeatedly (11th time now), no go-ahead given, continue withholding until explicit confirmation

## Update 14:21 UTC — fifth pre-compaction flush, same day

### New work since 13:49 entry: full userflow exploration in progress, validators dispatched
Ignat asked (13:49) for a complete fix list based on all exploration so far, PLUS explicitly flagged a gap: no feedback yet on Manual mapping and other unexplored options. Asked to click all buttons, explore all userflows, review with validators, aiming to improve the userflow overall (not just the create-reconciliation overlay).

**Progress reported to Ignat before this flush:**

1. **Closed a long-standing open item:** "See details" on an Error-status row DOES correctly link to the Process log (`/processLog/rec_…`). This resolves the "unverified — does See details actually route to Process log" item that's been carried in memory since early August. **No longer open — mark resolved.**

2. **Confirmed partial production rollout of the list restructure** (the one specified in the Jira task body drafted back in early August) — found in the **"Per transaction test"** org:
   - Single Status column with Error pills ✅ shipped
   - Matched % column removed ✅ shipped
   - Single Status filter with exactly the 4 correct options (In progress / Error / Attention required / Reconciled) ✅ shipped
   - Date range sort active (black), Date created grey (inactive-but-sortable) ✅ shipped, matches locked spec
   - **Gap found:** "See details" exists but is tucked inside a "···" overflow menu, not as a visible end-of-row link/button as originally specced. Needs a decision — was this a deliberate implementation choice or a miss?

3. **New terminology drift found across 3 different surfaces for the same action:**
   - List page CTA: "Run reconciliation"
   - Different org's empty state: "Run audit"
   - Create overlay's submit button: "Start matching"
   - Also: page title "Transactions reconciliation" (plural) vs sidebar nav "Transaction reconciliation" (singular)

4. **Real failure encountered during flow testing (not a copy nitpick — a functional/error-handling bug):**
   - Error message: "Couldn't build a reconciliation / Connection with your QuickBooks Online company has been lost. To run automated modes of reconciliation, please, reconnect the integration."
   - Problem: blames "the integration" generically when the actual broken connection is specifically QuickBooks Online (the accounting side, not the sales integration side)
   - No link/CTA provided to actually reconnect — dead end for the user
   - Both Accounting and Integration columns show stage-1 "Failed" even though only the QBO (accounting) connection was lost — over-broad failure state, misleads about scope of the problem

**Validators dispatched:** UX, Clarity, and Domain validators are running against 25 captured states from this exploration session. Consolidated fix list to be sent once they report back — not yet complete.

### Session state at this flush (14:21 UTC)
Validators still running / not yet reported. This flush is a pure continuity marker — no new decisions or additional findings since the message sent just before this event. Manual mapping-rules flow specifically still needs deeper exploration per Ignat's explicit callout ("I don't see any feedback on manual mapping and other stuff") — this is flagged as incomplete/outstanding, not yet delivered.

### Important context for continuation
- Ignat's ask is broader than the create-overlay review from earlier today — now scoped to the FULL reconciliation userflow (list → create → detail/results → error states), with validator review requested
- Do NOT send the final consolidated fix list until validator output is in — Ignat is waiting for the validated version, not a preliminary one
- Manual mode mapping-rules flow exploration is a known gap that must be covered in the final deliverable
- 25 states already captured this session for validator review — check for a corresponding captures folder (likely under `.synder-state/` following the pattern from earlier today, e.g. `.synder-state/recon-create-2026-08-19/`) when resuming

### Standing items — status update
- **RESOLVED:** "See details" → Process log link — confirmed working, remove from open-items tracking going forward
- List restructure — mostly shipped, "See details" placement (overflow menu vs. visible row button) needs Ignat's call
- Terminology drift (Run reconciliation / Run audit / Start matching / plural-singular page title) — new finding, needs consolidation into final fix list
- QBO disconnection error handling — new functional bug finding, needs consolidation into final fix list
- Manual mapping-rules flow — still unexplored as of this flush, explicitly requested by Ignat, must be completed
- Import method vs Statement source naming decision — still awaiting Ignat's final call (carried from 13:49 entry)
- DIS-259 PROJECTS.md registration — still not done, flagged repeatedly (12th time now), continue withholding until explicit go-ahead

## Update 15:14 UTC — sixth pre-compaction flush, same day

### New work since 14:21 entry: full consolidated fix list (F1-F16) delivered, twice
Validators reported back (UX clean; clarity/domain agent died mid-run, assistant did that pass manually against vocabulary.md instead). Delivered a complete consolidated fix list covering the full reconciliation userflow exploration from today. Message was sent twice (15:01 and again at 15:02 after Ignat said "hey" — likely first delivery didn't land / Ignat didn't see it). **Full canonical fix list — record permanently, this is the deliverable for today's entire exploration session:**

**🔴 Flow-breaking**
- **F1** — "Any account" removes the Integration-side mode selector entirely (no way to supply integration data), but Start matching stays enabled — user only discovers failure after submit via Process log. Fix: keep selector defaulted to Automated, or block submit with inline reason.
- **F2** — QBO-disconnected error blames "the integration" (sends user to reconnect Stripe) when the actual broken connection is QuickBooks; no reconnect link, no retry; both columns wrongly show "Failed" when only QBO broke. Fix: rewrite message to name QuickBooks Online specifically, add Reconnect QuickBooks Online + Retry actions, change unaffected column to "Cancelled".
- **F3** — X-close discards the whole form with no confirmation, including files just exported from Stripe. Fix: confirm when form is dirty.

**🟠 Flow**
- **F4** — Two selectors both labeled "Automation mode" with different option sets (Accounting: Automated/Manual; Integration: Automated/Assisted/Manual) — users miss the second one entirely, one side silently runs on an unchosen default.
- **F5** — Manual mode has zero file guidance (vs. Assisted's per-file 5-step instructions) and no link to the mapping rules it references.
- **F6** — Get-started panel's numbered steps (1-date/2-integration/3-mode) contradict actual form reading order (date→mode→integration).
- **F7** — Get-started panel is dismissible with no way to reopen it — the page's only orientation aid.
- **F8** — "See details" implemented inside "···" overflow menu, not as visible end-of-row link as originally specced.

**🟡 Copy**
- **F9** — Rename "Automation mode" selector → **"Import method"** (final recommendation, reasoning: matches vocabulary.md's approved "Import" verb, accurately covers all 3 modes, works on both asymmetric option sets). Update Get-started panel step 3 to match.
- **F10** — Three different labels for the same action across surfaces: "Run reconciliation" (list) / "Run audit" (empty state) / "Start matching" (create). Standardize on **"Run reconciliation"**.
- **F11** — "Transactions reconciliation" (page title, plural) vs "Transaction reconciliation" (sidebar, singular) — use singular.
- **F12** — "mapping rules" link opens a modal titled "Matching rules" — terminology mismatch, align both to "matching".
- **F13** — Same modal's tables headed "Accounting"/"Payment" (a 4th name for the integration side) plus "Missing in payment platform" vs. results tab's "Missing source" — inconsistent naming across surfaces.
- **F14** — "Reconciliation details" title shown while creating → should be "New reconciliation".
- **F15** — Stray comma in "please, reconnect the integration".
- **F16** — "Cancelled" status on unaffected column reads as user-initiated → rename to "Skipped".

**"Any account" replacement — recommended approaches:**
1. Best: lift the decision out of the Integration dropdown entirely — new top-level question "What do you want to reconcile?" → "An integration" / "A GL account"
2. If kept inside the dropdown: rename to "No integration — reconcile a GL account" (current "Any account" wording misleadingly reads as "all accounts", the opposite of its actual function)
3. Either way, add helper text: "Reconcile a general ledger account that isn't connected to an integration. You'll upload the account statement yourself." — this helper text also functionally resolves F1 by setting correct expectations upfront

**Captures:** 25 states saved to `.synder-state/recon-create-2026-08-19/` for reference.

**Open offer, still unanswered:** Assistant asked twice (end of both 15:01 and 15:02 messages) whether to (a) re-run clarity/domain validators properly for a second opinion on copy items, and (b) register DIS-259 in PROJECTS.md while context is fresh. **No response from Ignat yet as of this flush.**

### Session state at this flush (15:14 UTC)
Pure continuity marker — no new decisions or work since the fix list delivery ~15:01-15:02 UTC. Today's entire reconciliation userflow exploration (login → create overlay analysis → corrections/retractions → mode dropdown discovery → full flow exploration with validators → consolidated F1-F16 fix list) is now considered **complete and delivered** as of this flush, pending only Ignat's response on the two open offers above.

### Standing items — final status update for today
- **RESOLVED today:** "See details" → Process log link (confirmed working)
- **RESOLVED today:** Assisted-mode upload guidance concern (confirmed already exists, just wasn't visible in the dropdown assistant initially checked)
- **RESOLVED today:** "Any account" purpose clarified (reconcile GL account without integration) — though naming/placement still flagged as F1 + copy fix
- **NEW open items from today, all captured in F1-F16 above** — this replaces/supersedes prior fragmentary open-items tracking for the create-reconciliation overlay specifically
- Import method vs Statement source naming — DECIDED as of 15:01/15:02 delivery: assistant recommended and Ignat has not yet objected to "Import method" (F9) — treat as the working recommendation, not yet fully confirmed-locked by Ignat
- **DIS-259 PROJECTS.md registration — still not done, offered explicitly at end of both 15:01 and 15:02 messages, still no go-ahead as of this flush (13th time flagged overall).** Continue withholding until explicit confirmation — do not add without it.
- Older/pre-existing balance-reconciliation-results-page open items (DOM-1 typo, label/tooltip mismatch, direction indicator, USD repetition, demo data variation) — untouched today, still carried forward unchanged from 2026-08-18 log

## Update 15:33 UTC — seventh pre-compaction flush, same day

### New work since 15:14 entry: Ignat requested clarity/domain validator re-run, dispatched
Ignat responded (15:14) to the two open offers from the previous message — chose to ask for clarity/domain agent feedback specifically (did not respond to the DIS-259 PROJECTS.md registration offer, which remains unanswered).

Assistant dispatched Clarity and Domain validators as **separate agents this time** (previous run at ~14:21-15:01 had the clarity/domain agent die mid-run, so assistant did that pass manually — Ignat now wants the actual agent-run feedback, not the manual substitute). Each agent was given:
- The validator persona/protocol file
- `vocabulary.md`
- 10 relevant screenshots (subset of the 25 captured today, presumably the most copy-relevant ones)
- The full set of verified copy strings from today's exploration (the F1-F16 list content)

Told Ignat findings will be posted once agents report. **As of this flush (15:33 UTC), validators are still running — no results yet.**

### Session state at this flush (15:33 UTC)
Pure continuity marker. No new findings, no new decisions since dispatching the validators ~15:14-15:15 UTC. This is purely a "still waiting" checkpoint — do not re-dispatch validators again if this flush repeats before they've reported; check actual agent/task status instead of assuming they need restarting.

### Standing items — status update
- **Clarity + Domain validator run — IN PROGRESS, not yet complete.** This is now the single active blocking task. When they report, consolidate findings and send to Ignat as a follow-up to the F1-F16 list (do not re-send F1-F16, only the new validator-specific findings).
- **DIS-259 PROJECTS.md registration — still unanswered, now 14th time flagged.** Ignat responded to the clarity/domain part of the double-offer but not this part. Continue withholding until explicit confirmation; do not re-offer repeatedly in every single message — it's been noted enough, just act if/when Ignat says the word.
- Import method (F9) naming recommendation — still standing as working recommendation, not yet explicitly confirmed-locked by Ignat, no update since 15:14.
- All other F1-F16 items and older balance-reconciliation-results-page items — unchanged, no new activity.

## Update 15:59 UTC — eighth pre-compaction flush, same day

### New work since 15:33 entry: validator findings delivered (manual pass, agents died again), self-reflection on workflow requested and delivered

**15:40 message — validator findings + F-item corrections:**
Ignat responded to F1/F2/F4/F5/F6/F7/F8/F12/F13/F16 individually. Key corrections to lock in:
- **F1 retracted** — with "Any account" the user CAN select both account and mode; assistant had this wrong, corrected.
- **F2 narrowed, not dropped** — "integration" is Synder's genuine umbrella term company-wide (used this way in onboarding, and generically as "Integration lost connection" for any disconnection). Ignat confirmed this IS the standard term. BUT: on the Reconciliation page specifically, the UI explicitly labels two separate columns "Accounting" and "Integration" — so when QBO (Accounting side) breaks and the message says "reconnect the integration," it points at the wrong column *in this specific context*, even though the word itself is correct app-wide. Residual part of F2 still valid regardless of terminology: no reconnect link, no retry action.
- **F4 retracted as a "bug"** — two selectors share the label "Automation mode" because they refer to the same underlying object/action (by design, not an error). Residual: Get-started panel's step "3. Choose automation mode" is singular when there are actually two instances — minor copy nit only.
- **F5 retracted** — Manual mode intentionally has NO guidance; that absence IS the defining difference from Assisted (Manual = reconcile any file, user's choice, no prescribed report). Not a gap.
- **F6** — Ignat said "okay for now" — deprioritized, not fixed, not urgent.
- **F7 retracted** — Get-started guidance is intentionally shown once; user doesn't need it repeated every time. Not a bug.
- **F8 retracted** — the whole table row is clickable (same pattern as other pages in the product) — "See details" being inside "···" isn't a placement bug, the row itself is the real click target.
- **F12, F13** — Ignat just said "thanks" — implicitly confirmed/accepted these copy-alignment findings (mapping/matching rules modal title mismatch; Accounting/Payment/Missing-in-payment-platform vs Missing-source terminology drift). Treat as accepted findings, no further action needed except eventual implementation.
- **F16** — Ignat said "I don't understand... Are you here?" — assistant explained: on the Process log, when stage 1 fails, stages 2-3 show "Cancelled" which reads as human-initiated when really they just never ran because the prior stage failed. Recommended "Skipped" or "Not started" instead. This explanation was given but Ignat's reaction to the explanation itself is not yet recorded (message stream moved on before confirmation).

**Manual validator pass delivered** (agents died on restart again — twice now — so assistant ran Clarity and Domain personas manually against their persona files):

**Clarity (business owner, ~$150K store, no accounting background):**
- **CLR-1 (Critical)** — "Account to reconcile" dropdown shows all 167 chart-of-accounts entries (e.g., Inventory Shrinkage, Accounts Receivable (A/R) - AUD, Stripe Lending Liability) with no guidance; wrong pick produces a meaningless reconciliation with no warning — likely the actual quit/abandon point on this page. Fix: preselect the account tied to the chosen integration, badge it "Recommended" like Automated mode is badged.
- **CLR-2 (High)** — list page has reassurance copy "Read-only comparison — no changes to your books" but the CREATE overlay (where the "Start matching" button actually lives) has no such reassurance at point of action. Fix: add "Read-only — this never changes your books." near the primary button.
- **CLR-3 (Medium)** — mode descriptions use jargon in one sentence ("mapping, normalization, and matching") that a non-accountant persona doesn't understand, yet it's the exact sentence used to choose a mode. Fix: simplify Assisted's description to "Upload the reports we ask for. Synder does the rest."

**Domain (senior accountant, 12+ years, daily QBO user):**
- **DOM-1 (High)** — on a failed run, BOTH Accounting and Integration columns show stage-1 "Failed" even though the banner names only the QuickBooks connection as broken — accountant can't tell if the Stripe/Integration side actually succeeded, so can't judge if re-running risks a double-pull. Fix: only fail the actually-affected side; mark the other "Skipped".
- **DOM-2 (High)** — with "Any account" selected, the Integration side is forced into Manual mode with a bare upload and no stated required artifact. Reconciling a GL account normally means uploading a *statement*, but the UI never specifies statement vs. transaction list — two accountants would upload different things and get different (wrong) results. Fix: label field explicitly "Upload the account statement for this period."
- **DOM-3 (Medium)** — Accounting column only offers Manual (no Assisted option), with no stated expected export either — even though the accountant usually knows exactly where this data lives (GL account register) and could be given clear export guidance. Fix: either add Assisted to the Accounting side with export guidance, or at minimum name the expected export in the Manual label.

**15:41 message — Ignat asked for meta-feedback: "You were overloaded with information. How can we simplify the job? Give suggestions on how to help you?"**

Assistant's self-reflection and concrete proposals delivered:

**What assistant will change going forward:**
1. Split future reports into "verified bugs" vs. "intent questions" — much of today's churn was really questions phrased as assertions (Any account behavior, Manual guidance, shared-label selectors) that could've been single-line-answered instead of triggering corrections.
2. Never review from stale/old captures again — reviewing from April screenshots against a page that had moved a full minor version caused several wrong findings. Always re-capture live and state the version being reviewed.
3. Verify every field value actually landed/registered before judging what's present or missing (reinforces lesson already logged twice today).
4. Run validator personas inline/manually rather than as background agents — the background agents died on session restart TWICE today, wasting two waits for nothing.

**Asks made of Ignat (not yet answered as of this flush):**
5. **Proposed writing a "by design" reference file** capturing settled conventions so they're never re-flagged: "integration" = umbrella term for all connected sources; desktop-only; grey/black = sortable/active convention; Manual mode = any file, no guidance, by design; column symmetry gaps intentional; unlimited date ranges allowed. Assistant offered to write this immediately from today's corrections — **awaiting Ignat's go-ahead.**
6. **Asked Ignat to designate one canonical review org** — today bounced between Summary_test / QBO sandbox / "Per transaction test", which have different sidebars, feature sets, and (critically) different connected-accounting-company states, directly causing the earlier wrong "Synder vs Accounting" terminology finding. **Awaiting Ignat's answer on which org to standardize on.**
7. **Flagged again: CF Access service token in `.synder-creds` is dead** — today's access only worked because an old CF cookie happened to still be valid, and that cookie **expires Aug 26, 2026**. This is a real upcoming deadline — if not resolved before then, future Synder access attempts will fail again. **Needs a fresh token before Aug 26.**
8. Asked Ignat to point assistant at relevant Figma/FDD docs when reviewing a specific area, since many "is this intentional?" guesses would be resolved instantly by the spec instead of by trial-and-error + correction cycles.

Closed with: smallest version of all this = target + org + "here's what's by design" + ask for "bugs and questions" rather than full audit → turns a full-day exploration into a ~20-minute focused task.

**Also re-flagged (again) at end of message:** offer to register DIS-259 in PROJECTS.md — now explicitly noted by assistant itself as "the sixth time I've flagged it" (assistant's own count, though actual workspace-log count is higher — see below).

### Session state at this flush (15:59 UTC)
No reply yet from Ignat to the 15:41 self-reflection/simplification message. Pure continuity marker.

### IMPORTANT — new deadline to track
**CF Access session cookie for demo.synderapp.com expires 2026-08-26.** This is a real, dated, upcoming access-loss event, not a vague "eventually" problem. If a fresh service token or alternate access method isn't arranged before then, all future Synder live-review sessions will be blocked again exactly as happened earlier today. Surface this proactively as the date approaches if Ignat hasn't addressed it.

### Standing items — final status update for this flush
- **F1, F4, F5, F6, F7, F8 — retracted/resolved/deprioritized per Ignat's corrections above.** Remove from active fix-list tracking; only residual notes (F2 reconnect-link/retry gap, F4 panel-text pluralization) remain actionable.
- **F12, F13 — accepted, pending implementation** (no further review needed, just need to be actually fixed/shipped eventually).
- **F16 — explanation given, Ignat's final reaction not yet confirmed** — revisit if Ignat responds further.
- **New validator findings (CLR-1/2/3, DOM-1/2/3) — freshly delivered, not yet responded to by Ignat** — awaiting his reaction/corrections, same pattern as F-items likely to follow (expect some retractions).
- **Three open asks awaiting Ignat's response:** (a) go-ahead to write the "by design" reference file, (b) designate one canonical review org, (c) provide Figma/FDD pointers when relevant.
- **DIS-259 PROJECTS.md registration — still not done.** Total flag count across the full day (per this memory file's cumulative log) is well into double digits — continue withholding until Ignat gives explicit go-ahead; stop proactively re-mentioning it in every single message since it's now thoroughly logged — a single subtle mention per major deliverable is enough, avoid nagging.
- CF Access token — see new deadline note above, now the most concrete/time-bound open item of the day.

---

## 🌙 End-of-day snapshot — 17:00 UTC / 20:00 Vilnius (scheduled 23:00 review)

### What today actually was
The single biggest working day in recent memory: one continuous DIS-259 deep-dive on the
**New reconciliation overlay** in the Synder demo app, plus a security finding, plus a
workflow retrospective. Eight pre-compaction flushes fired between 11:24 and 15:59 UTC.

### Delivered today
1. **ETC notifications closed out** — the three open questions carried since 2026-08-05
   (system-health alerts in dispatcher bell, filtering at 100/day volume, actionable-vs-
   deletable) are resolved in-product per Ignat. Removed from open items.
2. **Berlin metro (PIMS/BVG) maintenance dialog copy** — recommendation delivered, then
   revised after follow-up on the retry button and night-time context.
3. **DIS-259 live exploration** — logged into demo.synderapp.com, walked the full New
   reconciliation userflow, captured 25 screenshots, produced consolidated fix list
   **F1–F16** (delivered twice).
4. **F-list triage with Ignat** — F1, F4, F5, F6, F7, F8 retracted/deprioritized after his
   corrections; F2 narrowed (residual: no reconnect link, no retry); F12/F13 accepted
   pending implementation; F16 explained, reaction unconfirmed.
5. **Clarity + Domain validator pass** — CLR-1/2/3 and DOM-1/2/3 delivered. Ran manually
   because background validator agents died on session restart twice.
6. **Workflow retrospective** — Ignat asked "how can we simplify the job?"; self-reflection
   plus four concrete process changes delivered.
7. **Two new process docs written (16:10–16:13 UTC)** — `personas/WORKFLOW_AS_IS.md`
   (validator pipeline, honest about which steps actually execute) and
   `personas/HOW_WE_WORK.md` (whole working relationship, channel constraints, six task
   shapes, friction on both sides). Both verified against repo, not recalled.
8. **Session triage** — diagnosed non-responsive Telegram topics: 7433 is at 314k tokens in
   a 200k window (over ceiling); 279 swallows messages that arrive exactly during a
   compaction (happened twice: 14:22, 15:03). Fix in both cases: send again.
9. **Mapping-page audit brief** — wrote the paste-ready kickoff message for Ignat to run
   that exploration in a separate topic.

### Open — awaiting Ignat
- Go-ahead to write the **"by design" reference file** (settled conventions so they're
  never re-flagged).
- Designate **one canonical review org** (today bounced between Summary_test / QBO sandbox /
  "Per transaction test" — directly caused a wrong terminology finding).
- Figma/FDD pointers when reviewing a specific area.
- Reaction to CLR-1/2/3 + DOM-1/2/3 (expect some retractions, same as the F-items).
- DIS-259 registration in PROJECTS.md — still withheld pending explicit confirmation.

### Open — needs action, dated
- **⚠️ 2026-08-26 — CF Access session cookie for demo.synderapp.com expires.** The service
  token in `.synder-creds` is already dead; today's access only worked on a stale cookie.
  No fresh token before that date = all live Synder review blocked. 7 days out.
- **🔐 `.github-token` is tracked in git** with a live `ghp_…` token (commits `b8ab132`,
  `d295108`). GitHub push protection is rejecting every push — **21 local commits unpushed**.
  Token has NOT reached GitHub. Not bypassed deliberately. Needs: rotate token + history
  rewrite + gitignore the four credential files. Awaiting Ignat's go-ahead (force-push).

### Lessons logged today (both reinforced twice)
- Never review from stale captures — re-capture live and state the version reviewed.
- Verify a field value actually landed before judging what's present or missing.
- Run validator personas inline, not as background agents — they died twice today.
- Split reports into **verified bugs** vs **intent questions**; much of today's churn was
  questions phrased as assertions.
