# 2026-08-27

## Pre-compaction flush — carry-over from Q.Daba session (2026-06-28 to 2026-08-25)

### Q-Explorer / Q.Daba prototype
- Live: https://dashasyn.github.io/synder-prototypes/projects/q-explorer-prototype/
- File: `projects/q-explorer-prototype/index.html`
- 12 views total: wizard, evaluations list, scheduled, Anschlusspünktlichkeit (V5), Fahrtausfälle DPM + Ausfallmaske + treemap (V8/V10), Pünktlichkeit DPM + chart modal + Pünktlichkeitsdaten raw (V11/V12). All 3 report views share grey-chip params bar (chips grey, some with ↓ expand arrows for TU's/Kantone/Linien, dashed for "Alle Linien").

### UX Validator Review (done 2026-06-29) — STILL UNAPPLIED as of 2026-08-25
9 findings never fixed, all still present in the file:
- UX-1: "Transport companies" still in English (should be Transportunternehmen), 3 places
- UX-2: CSV Export button on V8 (Fahrtausfälle) has no onclick handler — only some of 4 CSV buttons wired
- UX-3: Only V11 has a "Schließen" button; V5/V8 don't
- UX-4: Generation timestamp subtitle only exists on V11, missing on V5/V8
- UX-5: No KPI summary row on V11 (Pünktlichkeit) unlike V5/V8
- UX-6: Label inconsistency — "Aufschlüsseln nach" (V5) vs "Aufschlüsseln" (V8/V11)
- UX-7: Duplicated CSS classes `.fa-params-bar` and `.rpt-params-bar` (identical styles, should merge)
- UX-8: Tag badge slot (`rpt-topbar-tag`) only exists in V5 header, not V8/V11
- UX-9: Not a bug — bordered field style intentionally used on raw/drill-down views (V10, V12) vs chips on summary reports (V5/V8/V11); worth documenting as intentional pattern, not drift

**Next session: ask Ignat if he wants these 9 findings cleared now.**

### Work completed 2026-08-25 (pushed live)
1. **Custom time period + scheduled reports fix**: Custom period selection now blocks scheduling — checkbox disabled, section greyed, amber warning note + "Zeitraum ändern" link back to step 1. If schedule was already checked and user switches to Custom, it auto-clears. Editing an existing scheduled report: Custom button itself locked with tooltip, Save Changes disabled (that flow can never accept a fixed range). Also fixed: clicking the schedule row (not just checkbox) now works.

2. **Login screen footer links (Impressum/Dokumente/Support/Kontaktdaten)**: Ignat referenced pattern from N8 (Israel project) — sent screenshot showing full-width bottom bar (not centered row): leading icon + label + external-link arrow per item, left group (Impressum/Dokumente/Kontaktdaten) + Support right-aligned. Q-Explorer restyled to match this N8 pattern exactly. Also added same 4 links as plain-label sidebar footer inside the app post-login (Ignat's own addition, not in N8 screenshot — flagged as optional/removable).
   - Each link opens an in-app modal (Impressum: BAV publisher/ETC operator + liability line; Dokumente: 4-row download list; Support: hours/mail/phone + "include timestamp+eval name" tie-in; Kontaktdaten: split BAV/ETC contacts)
   - All invented legal/contact copy marked as placeholder in dashed box — explicitly not signed-off client content

### OPEN QUESTION — blocking, needs Ignat's answer
Sent 2026-08-25, unanswered: the N8 screenshot links all show an external-link arrow (↗) with copy like "Visit ETC Solutions" / "Visit ISR" — implying they navigate away to external URLs. But in Q.Daba's prototype, all four (Impressum/Dokumente/Support/Kontaktdaten) currently open **in-app modals** instead, since there's nothing to link to yet.
**Need Ignat to clarify:** Are these meant to be real external URLs (keep arrow, drop modals) or in-app pages (drop the arrow)? This determines whether the current modal implementation is correct or needs to change to real links/removal of the ↗ icon.

### No new instructions this turn
This turn (2026-08-27 08:44 UTC) was a system-generated pre-compaction flush event, not a user message. No action needed beyond capturing memory. Correct response: NO_REPLY.

## Q-Explorer (Q.Daba) Round 1 review — 08:44–09:15 UTC

Ignat: "Review q-explorer prototype. Check all pages, few userflows. I want to know how does it work.
Is it easy to understand." Ran VALIDATOR_PROTOCOL v2 rather than improvising.

**Artifacts:** `projects/q-explorer-prototype/review-2026-08-27/` — BRIEF.md, round1/ + round2/
(statemap.json, manifest.json, auto-findings.json, validator payloads, 21 screenshots), published
report `index.html`. Commit `72e2c2a`, pushed.

**Recon** (real Chromium, all 13 views, 3 flows, DE+EN). Both statemaps PASS the commit-path gate.
**Validators:** UX×3 + Clarity + Trust ran = 22 findings, all schema/evidence-clean. **A11Y never ran
and round 2 got no validator pass at all** — sessions_spawn started returning "Unable to connect"
after the 5th child (cap is 5 active) and never recovered. `verify` fails round 1 on `MISSING · a11y`.
Recorded as a failed round in the report instead of quietly re-declaring the manifest without a11y.

**Corroborated Criticals (all three UX instances independently, + Trust/Clarity):**
1. **Clear-✕ sits at the horizontal centre of every filter trigger.** `.custom-select-trigger` uses
   `justify-content:space-between` with three children → `elementFromPoint(centre)` returns
   `canton-clear`. Clicking the middle of a filled filter to reopen it wipes the value instead:
   verified `['AG']`→`[]` and `['BLS']`→`[]`, panel doesn't open, no message, no undo. One-line fix.
2. **Edit-a-schedule loads nothing.** Wizard titled "Fahrtausfälle DPM – Wöchentlich" + Scheduled
   badge, but state = defaults (`cur_month`, empty TU/cantons/lines) and the schedule section is
   `display:none` so frequency can't even be seen.
3. **10 of 19 "Fertig" evaluations can't be opened.** Only 9 of 28 rows have Anzeigen; row name not
   clickable. All Rohdaten Export / Linienanalyse / DQI rows are dead ends.

**Found by my own recon + deterministic pass (NOT independently validated — flagged as such):**
- Both "Aufschlüsseln" controls are inert. V5's three selects have no `onchange` at all; V11's handler
  only drives option-disabling. Table stayed byte-identical across every breakdown change.
- 27 raw-data column search inputs + 3 selects have no handler. Typing a real value + Enter changes nothing.
- Ausfallmaske "Anwenden"/"Zurücksetzen" have no onclick — that filter form has no commit or reset.
- V8 CSV Export, treemap camera, chart Snapshot + fullscreen: no onclick (V5/V11 CSV do work).
- **Bundle-row expander: first click does nothing**, second expands (20→20→26→20, reproduced 4×).
  Cause: button ships without `.collapsed` while rows ship hidden. Caret icon never flips. Same class
  as MISSES.md PROTO-2.
- Chart popup: Escape doesn't close, focus never enters, not trapped (the new Info modals DO handle Esc).
- 37 `data-i18n` keys missing from BOTH dictionaries → EN mode keeps German (Linienbündel, Haltestelle,
  Monat, TU konzessioniert/fahrend ×3 selects).
- 109 hex values (41 used once), two grey ramps mixed, 13 font sizes, 10 radii; `.rpt-params-bar` and
  `.fa-params-bar` byte-identical. Contrast: #9CA3AF 2.54:1 used 40× as text colour.
- Reversed date range accepted silently; login accepts empty credentials; type cards are divs with no
  tabindex (entry point unreachable by keyboard); sidebar shows no active item inside a report.

**Positive and worth protecting:** the Pünktlichkeit arithmetic is fully consistent (Delta = Soll−Ist,
Wert% = pünktlich/Ist on the Total row and 5 bundle rows). Auto-generated evaluation name is the best
feedback mechanism in the tool. Landing on the list (not a form) is right. Pagination over 4 950 rows
works. KPI cards with a 90% threshold are the strongest design element — which is why V11 having none stands out.

**Measurement mistakes I made and corrected before reporting** (worth remembering):
- `textContent('.page-title')` grabs the first hidden view's title → always scope to `.view.active`.
- Wrong class guesses produced two false "missing" findings: KPI cards are `.rpt-kpi-card`/`.fa-kpi-card`
  (not `.kpi-card`), chip dropdown is `.rpt-chip-dropdown` (not `.rpt-chip-dd`). Both "absent" readings
  were artifacts.
- `showRptApplyToast` writes to `#rpt-snackbar`, not `#app-toast` → "V5 CSV does nothing" was wrong.
- Swiss number format: `14.048` is 14 048, not 14.048 — my first arithmetic check falsely failed.
- Re-querying the same selector after a handler swaps the element reads as "button gone", not "no change".
- Playwright dismisses native `confirm()` by default → delete looked like a no-op until I added a dialog handler.

**Still open:** re-run A11Y + the round-2 lenses when sessions_spawn recovers; nothing from the review
applied yet; the 29 June review remains unapplied and is folded into this report.
