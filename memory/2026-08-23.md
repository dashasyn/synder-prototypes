# 2026-08-23

## Snapshot (23:00 Vilnius / 20:00 UTC, scheduled end-of-day review)

**No work session today — second quiet day in a row.** No commits, no prototype edits, no messages
from Ignat, no reviews run. Last commit is still `227aa8d` (yesterday's end-of-day snapshot).
Working tree holds automation churn only (`DREAMS.md`, `MEMORY.md`, `memory/.dreams/*`,
`morning-ideas.json`, `.synder-state/storage-state.json`, plus untracked CLI image cache).

Everything that touched the workspace was background automation:

- `03:00` — dreaming pass wrote `memory/dreaming/{light,deep,rem}/2026-08-23.md`, appended to
  `DREAMS.md` and `memory/.dreams/events.jsonl`, rotated the 08-21 and 08-22 session corpora.
- `03:00` — MEMORY.md promotion block rotated. **The drift flagged yesterday did not repeat.**
  Today's block promotes six entries and four of them are real product content — the 08-17 weekly
  friction report (dead clicks, the broken reconciliation funnel, the Sync Issues +23% signal, the
  Galileo API notes). One is still a snapshot meta-note. So the scorer was starved, not
  mis-weighting; it recovered as soon as substantive material was in range. Closing that watch item
  unless it returns.
- `07:08` — morning-ideas wrote entry 90: **unblock the connect-step copy by making it
  timing-agnostic.** The Stripe/Square write-access bullet has been stuck since 10 Aug on one
  unanswered eng question (is the Stripe payment created at invoice *send* or at customer *pay*
  time — asked 10 Aug and 21 Aug, no answer). The current wording commits to pay time, which is the
  only reason the answer gates it. Rewrite to "Record payments in Stripe for invoices you send from
  your books" and it is true either way; the safety bullet already avoids timing and needs no
  change. What earns trust at that moment is the *scope* claim (only your own invoices, never
  spontaneous), not the internal timing of the payment object — which a merchant cannot observe
  anyway. Recommendation: ship the timing-agnostic copy now, treat the eng answer as an optional
  refinement rather than a blocker. Thirteen days of a copy deck held hostage for nothing.

## Carried forward, unchanged (nothing closed today)

**Filters / transactions-list thread** (live: https://dashasyn.github.io/synder-prototypes/filtering-options/)
- Reset label naming: "Clear filters" vs "Clear all" — pick one.
- V6 asymmetry: segments commit instantly while status needs Apply (my call, not Ignat's).
- V5 presets commit-on-click vs staging.
- 8-group vs ~19-status taxonomy — product decision; also gates the pills-vs-dropdown fork on the
  earlier "variant 8" tab/group prototype.
- Whether to delete the drifted `reports/filtering-options/index.html` mirror.
- "Clear filters" focus-loss a11y fix (from 08-22 morning-ideas): the button removes itself the
  instant it is used, dropping keyboard focus to `document.body`. Two-line fix — move focus to the
  filter bar, or keep the button mounted-and-disabled. Same self-removing shape on the V7 side
  sheet close action; check both together. Also unverified: at ~768px viewport height or 150% zoom,
  the bottom of the filter panel may sit under the sticky footer, putting Apply/Clear out of reach.
  One screenshot answers it.

**Onboarding connect step (SET-493)** — canonical copy in `onboarding/connect-step-copy.md`
- Stripe payment timing question: still unanswered after 13 days and two asks. **Today's idea makes
  it non-blocking** — see above. Decision for Ignat: ship timing-agnostic wording, or keep waiting.
- Six screen fixes flagged 08-21 and still not fixed: colon inconsistency across the three screens,
  "XCart" tab vs "X-Cart" field labels, missing `?` tooltip trigger on the QBO first bullet, missing
  Synder logo in the QBO sidebar, QBO tooltip anchoring to the QuickBooks logo instead of its
  bullet, and the "…don't exist yet" wrap stranding "yet" + icon on its own line.

**Settings & Billing rework** — 3 published layouts; add-on prices still placeholders and the
Member-role spec is still self-contradictory.

**Weekly friction metrics** — two metrics still need rebuilding before the next run: element-level
rage clicks (Galileo built it with a frozen-frame filter and returned 0) and the reconciliation
funnel (wrong entry step + two URL families). Asked Ignat, no answer.

## Lesson reinforced (no new one today)
Nothing today tested the CSS-port or assert-visibility rules. The promotion-drift hypothesis from
08-22 resolved in favour of "starved, not broken."
