# 2026-08-03

## Context carried from recent sessions (pre-compaction flush)

### Workflow evolution discussion (2026-07-24 to 2026-07-31)
- Ignat wants to improve: design skills, agent/validator usage, automation, Figma token economy, prototype fidelity to real Synder
- **Weekly LogRocket friction report** — set up as cron, Mondays 10:00 AM Vilnius time, delivered to Telegram. First run 2026-07-27 worked (with Galileo rate-limit issue on 2nd/3rd query — need to space queries ~30s apart).
  - First run findings: rage clicks on Daily Summaries "Synced" filter + "Apply filter" button; Accounting module "Select All/Apply/Submit"; dashboard role selector/Admin toggle; Daily Summaries mapping page. Matches previously-found filter/tab friction pattern.
- **Validator workflow confirmed**: run 3 parallel instances of the same validator on a design; findings caught by 2+ instances = higher confidence. This rule should be treated as active workflow policy.
- **IMPORTANT GAP FOUND (2026-07-31)**: Validators are NOT saved as real agent/skill definitions anywhere — they've been improvised fresh each time from memory, causing inconsistent depth. Agreed plan (awaiting build):
  - 5 proposed validators: Copy/vocab (CLR-n), Flow (FLW-n), States (STA-n), Component (CMP-n), Data trust (TRU-n — new, catches UI lying to users e.g. stale indicators, silent overrides, caught the FLT-2 filter bug)
  - Deepen LogRocket cron: rotate weekly focus instead of shallow-everything (Wk1 onboarding+first sync, Wk2 summaries+filters, Wk3 reconciliation, Wk4 settings+mapping); space queries 30s apart
  - Proposed second cron: monthly deep-dive cross-referencing LogRocket friction + open Jira tickets + component library gaps
  - **Next action when resumed**: build the 5 validators as saved skills (Ignat asked "Continue" — this was the last pending task before compaction)

### Synder HTML Component Library — built 2026-07-24, refined 2026-07-27
- Live at https://dashasyn.github.io/synder-prototypes/reports/synder-components.html
- File: `reports/synder-components.html` (13 sections: typography, buttons, inputs/selects, status chips, alert banners, tabs, filter bar, data table, sidebar nav, top bar, page header, accordion, full app shell)
- Focus pages requested by Ignat: **Transaction reconciliation (list/details/results)** and **Balance reconciliation** — these should be the reference source for tables/filters/headers, not generic pages
- Fixes applied 2026-07-27 after Ignat's review:
  - Subtitle1 = 16px bold black (was gray); Body1 = 16px regular black; Body2 = 14px regular for table cells/secondary text
  - Caption (12px) and Overline (10px uppercase) marked "NOT used in current Synder UI" — Synder doesn't use small fonts anywhere
  - Buttons: no uppercase/capslock — normal capitalization
- Still open: sidebar icons are emoji placeholders, need real Material Icons; table row density unverified; missing components noted as gaps: modals, tooltips, breadcrumbs, date pickers, toast notifications
- Root cause of prototype/production fidelity gap identified: components were being rebuilt from scratch per prototype instead of reusing extracted real DOM/CSS — this library is the fix, use "App Shell" section as base for new prototypes

### Credentials updated
- **GitHub token rotated 2026-07-29** — new token stored in `.github-token` (gitignored, not committed, not in any memory file). Do not print full token in chat logs going forward.

### AI-first prototyping trend (discussed 2026-07-24, for reference)
- Industry shift: Figma becoming a finishing/handoff tool rather than exploration tool; interactive prototyping moving to AI-generated code first (v0.dev, Bolt, Lovable-style workflow), Figma used at the end for dev specs/tokens
- Ignat's current workflow (describe intent → Dasha generates HTML prototype → iterate → Figma only for handoff) already matches this leading-edge pattern

## Open loops / next actions for future sessions
1. Build the 5 validators (Copy/vocab, Flow, States, Component, Data trust) as saved skill/agent definitions so review depth is consistent instead of improvised each time
2. Rotate weekly LogRocket cron focus per the 4-week plan above; fix query spacing to avoid Galileo rate limit
3. Consider building the proposed monthly deep-dive cron (LogRocket + Jira + component-library gap cross-reference)
4. Fix sidebar icons in component library (emoji → Material Icons), verify table row density, add modals/tooltips/breadcrumbs/date-picker/toast components

## Correction logged 2026-08-03 10:33 UTC — Validator system status

**I was wrong earlier in this same session** and corrected it: I initially told Ignat the validators "don't exist as saved definitions anywhere" (see entry above from 2026-07-31 discussion). This was FALSE and I retracted it.

**Actual state of the validator system (confirmed by direct file check):**
- System exists, saved, thorough: `personas/VALIDATOR_PROTOCOL.md` (173 lines)
- 4 validator definitions live in `personas/validators/`:
  - **UX Validator** — cognitive overload, hierarchy, friction, affordances
  - **Domain Validator** — accountant persona, terminology correctness, data integrity risk
  - **Clarity Validator** — first-time business owner, jargon, quit-risk moments
  - **Fidelity Validator** — QA engineer, prototype vs. Figma/Jira spec
- Each has hard limits: max 3-5 findings, 70-75% confidence floor, explicit "do NOT report" lists
- Protocol includes: auto-scan of interactive elements, browser interaction testing, style-deviation check vs `DESIGN_RULES.md`, Jira auto-fetch, dedup, 3-iteration cap, auto-publish
- **Correct trigger phrase**: `Review: [name or URL]` or `Build + Review:` for new prototypes — NOT "Review with validators" (Ignat's remembered phrase doesn't match exactly, but the intent/system is real and correctly captured)

**Root cause of my earlier wrong answer**: I searched only `skills/` and `.claude/agents/` (standard skill locations) and found them empty, then concluded validators weren't saved. The real files live in `personas/` and `personas/validators/` — I didn't check there before answering Friday. This was a sloppy verification step, not a system gap. **Lesson: always check `personas/` directory specifically before claiming something isn't saved — don't rely on standard skill locations only.**

**Two genuine gaps identified (real, still open) — NOT the same as the false "doesn't exist" claim:**
1. **No Data Trust validator** — none of the 4 current validators clearly own "does the UI lie about its own state?" The FLT-2 bug (dropdown showing "Failed" while displaying Ready-to-sync rows) didn't fit UX/Domain/Clarity/Fidelity scopes cleanly. Proposed new validator to close this gap (codes TRU-n).
2. **Validators don't see LogRocket data** — they evaluate designs in isolation from real user friction data. Proposal: wire in known friction patterns (e.g., Reload button = #1 rage-click magnet, import→sync funnel 97.2% drop-off) so validators check against real behavior, not just first-principles reasoning.

**Status: awaiting Ignat's go-ahead** to (a) add the Data Trust validator and (b) wire LogRocket friction data into the protocol. Not yet built as of this session.

## Skills migration discussion 2026-08-03 10:39 UTC — proposed, awaiting go-ahead

Ignat asked whether validators should be saved as skills/agents instead of personas, and requested a workflow suggestion + relevant Claude skills.

**Findings from investigation:**
- `DESIGN_RULES.md` exists ✅
- `findings-log.json` has **never been created** — meaning resolved-tracking and delta-mode (round 2 not re-litigating round 1) parts of the protocol have never actually run in practice. Every review round re-flags from scratch.

**My recommendation (given, not yet built):**
Move validators from `personas/` to a single skill (not 5 separate skills) because the real problem was discoverability — `personas/` isn't auto-loaded into context each session, so I have to remember it exists (this is literally what went wrong on Friday 2026-07-31). Skills ARE auto-listed every session.

Proposed structure:
```
design-review/                    ← skill, auto-listed every session
├── SKILL.md                      ← the orchestration protocol
└── references/
    ├── ux-validator.md
    ├── domain-validator.md
    ├── clarity-validator.md
    ├── fidelity-validator.md
    ├── trust-validator.md        ← NEW: "does the UI lie about its own state?" (catches FLT-2-class bugs)
    ├── design-rules.md
    └── known-friction.md         ← NEW: LogRocket findings validators check designs against (e.g. Reload button = #1 rage-click magnet, import→sync funnel 97.2% drop-off)
```
- One skill, not five, because validators always run together as a set through the protocol, never independently
- Trigger phrase stays the same: `Review:` / `Build + Review:` — no change needed to Ignat's workflow habit
- findings-log.json should actually get written this time so round 2 reviews build on round 1 instead of re-scanning from scratch

**Other Claude skills recommended as relevant (of all available):**
- `oracle` — second-model independent review, catches my own blind spots on findings
- Charting/dataviz capability — weekly friction report is currently wall-of-text, charts would help trend readability
- Explicitly NOT recommended: notion, meme-maker, tmux, most bundled skills — not relevant to UX research work

**Status: proposal given, awaiting Ignat's explicit go-ahead to actually build the `design-review` skill migration.** Nothing has been moved/created yet as of this message — this is the next concrete build task when confirmed.

## Session flush note
This is a pre-compaction memory flush pass triggered by "Continue the OpenClaw runtime event" system messages appearing repeatedly (2026-08-03, three times: 10:29, 10:33, 10:39 UTC). These are runtime-internal continuation prompts, not real user messages requiring a conversational reply — correctly handled by writing memory and not replying, or replying NO_REPLY when nothing new to add. Real user messages in between (about subagents, validators, skills workflow) were answered normally.

## design-review skill built 2026-08-03 10:39-10:47 UTC — PENDING INSTALL APPROVAL

Ignat said "Create design-review skill." I built it (validated, scan clean) as a **pending proposal awaiting install approval** — not yet applied/live.

**Proposal ID: `design-review-20260803-64e918e398`**

**Contents — 7 files:**
- `SKILL.md` — the 8-step orchestration protocol
- 6 reference files:
  - `ux-validator.md` — migrated as-is from personas/validators/
  - `domain-validator.md` — migrated as-is; now explicitly encodes one-org-one-platform as permanent (won't suggest platform-switching features)
  - `clarity-validator.md` — migrated as-is
  - `fidelity-validator.md` — migrated as-is
  - `trust-validator.md` — **NEW**. Codes `TRU-n`, max 4 findings. Scope: stale indicators, silent overrides, duplicate controls on one dimension, phantom progress, optimistic lies, hidden defaults, count mismatches, ambiguous bulk scope. Core question per control: "what does this claim / what's actually true / does it go stale when something else changes / what shows on failure?" This is the validator that would have caught FLT-2.
  - `known-friction.md` — **NEW**. Real LogRocket data as KF-1 through KF-9, validators now check designs against measured user behavior instead of pure first-principles reasoning. Baked-in examples: KF-1 Reload button hammering/rage-clicks, KF-2 import→sync funnel 97.2% drop-off, KF-3 date-picker arrow spam, KF-4 dead stepper labels.
  - `design-rules.md` — corrected version: fixed type scale (Subtitle1 16px bold black, Body1 16px regular black, Body2 14px regular), fixed palette, "no uppercase buttons" now explicit forbidden pattern, caption/overline marked do-not-introduce (matches Ignat's 2026-07-27 typography feedback)

**Also fixed/addressed in this build:**
- `findings-log.json` writing is now explicit **Step 7.3** in the protocol, with a note "this has historically been skipped, do not skip it" — directly fixes the delta-mode/resolved-tracking gap discovered earlier this session
- Multi-instance rule refined: UX validator runs 3× only on high-stakes reviews (corroborated findings ranked first); NOT all 5 validators triple-run since the 3-4 finding-cap validators would be wasteful to triple
- Reports must include a "What works" section and must honestly state any skipped steps
- Trigger phrases: `Review:`, `Build + Review:`, plus natural variants "review with validators" / "check this design" all now map to the same protocol — covers Ignat's remembered phrasing even though it wasn't the original exact trigger

**STATUS: NOT YET INSTALLED/APPLIED.** Waiting on Ignat's explicit go-ahead ("say the word and I'll apply it"). Once applied, `personas/` becomes the archive and `design-review` skill becomes the live path — this swap has NOT happened yet.

**Next action when resumed:** ask Ignat if he wants to apply/install the `design-review` skill proposal (ID above), or check if he already responded to the install prompt.

## design-review skill revised 2026-08-03 10:49 UTC — trigger no longer requires URL

Ignat's feedback: he usually works on screens/prototypes already in the conversation and wants to trigger a review without typing/pasting a link.

**Revision made to the same pending proposal (ID unchanged: `design-review-20260803-64e918e398`):**

**New Step 0 added to SKILL.md: "Resolve the target"**
- Triggers now include bare `Review`, `Review this`, `Check this`, `Check the design` — no URL required
- Resolution order when no explicit link given:
  1. The prototype already being discussed in the current conversation (normal case)
  2. The file most recently edited this session
  3. Newest modified prototype across `projects/prototypes/` and `reports/`
  4. Ask — only if genuinely ambiguous, and only ONE clarifying question
- Guardrail: every report now opens with the resolved target + how it was picked, e.g.:
  ```
  Round 1 — Platform Transactions v2
  Target: projects/prototypes/platform-transactions/index.html (most recent edit)
  ```
  This lets a wrong guess get caught in one line instead of discovered after a full review runs.
- Explicit rule baked into the skill: never stall on a missing URL when context makes the target obvious
- Also handles unpublished local files — publishes first so validators can fetch it live, rather than pasting raw HTML into prompts

**STATUS: still pending install, not yet applied.** Same install command as before:
```
openclaw skills workshop apply design-review-20260803-64e918e398
```
No working approval route existed in the chat session to apply it directly (confirmed 10:47 UTC exchange) — Ignat needs to run the CLI command himself, or explicitly ask to enable `skills.workshop.approvalPolicy: "auto"` (declined to set this unilaterally, offered but not actioned).

**Next action when resumed:** check whether Ignat has run the install command yet; if not, the `design-review` skill is still just a proposal sitting in the workshop queue, and `personas/validators/` + `personas/VALIDATOR_PROTOCOL.md` remain the only live/working validator system in the meantime.

## Approval blocker explained simply 2026-08-03 10:56 UTC — still unresolved

Ignat didn't understand the approval-policy explanation, asked for it in simple terms. Gave plain explanation:
- Installing a skill changes future behavior, so OpenClaw requires human approval before install
- The approve button/flow isn't working in this Telegram session right now, so the install request has nowhere to go — it's stuck, not broken/lost
- Two options given: (A) Ignat runs `openclaw skills workshop apply design-review-20260803-64e918e398` himself in a terminal, or (B) Ignat explicitly says "turn the approval off" and I set `skills.workshop.approvalPolicy: "auto"` — framed clearly as his call to make, not mine, since the safeguard exists specifically so I can't quietly change my own instructions
- **STATUS: still awaiting Ignat's choice (A or B).** Nothing installed yet. `design-review` skill proposal (ID `design-review-20260803-64e918e398`) remains pending/unapplied as of 2026-08-03 13:12 UTC.

## Session summary — 2026-08-03 (for next session pickup)

Today's session covered, in order:
1. Answered "what's my profession" recap
2. Deep workflow-improvement discussion (agents/validators/automation/Figma tokens/AI-first prototyping/fidelity gap) — carried over from prior days, resolved into concrete decisions
3. Corrected a false claim from Friday 2026-07-31 that validators "don't exist" — they do, in `personas/` (I'd only checked `skills/` and `.claude/agents/`)
4. Identified 2 real gaps: no Trust/Data validator, validators blind to real LogRocket friction data
5. Proposed and got approval to build `design-review` skill — built successfully, security-scanned, clean
6. Revised trigger logic per Ignat's request so reviews don't require pasting a URL — added Step 0 "Resolve the target" with fallback chain (conversation context → most recently edited file → newest prototype → single clarifying question)
7. Hit install blocker: no working approval route in this Telegram session for `skills workshop apply`. Explained twice (technical, then simple). Gave Ignat two clear options (run CLI command himself, or explicitly authorize `approvalPolicy: auto`). **Awaiting his decision — nothing installed.**

**Single most important open item for next session:** the `design-review` skill (proposal ID `design-review-20260803-64e918e398`) is fully built and ready but NOT installed. Check if Ignat ran the install command or wants approvalPolicy set to auto. Until resolved, `personas/validators/` + `personas/VALIDATOR_PROTOCOL.md` are still the only live validator system — do not assume the new skill is active.

## Install decision made 2026-08-03 13:12 UTC — Option A chosen

Ignat chose **Option A**: he will run the install command himself in a terminal, rather than authorizing `skills.workshop.approvalPolicy: "auto"`.

Gave him the exact command again:
```
openclaw skills workshop apply design-review-20260803-64e918e398
```
Told him to report back when done so I can verify the install (check `skills/design-review/` files landed, skill shows up in my listing).

**STATUS as of 13:21 UTC: still awaiting confirmation that he ran it.** Do not assume installed — verify by checking `skills/design-review/` directory exists with the 7 files (SKILL.md + 6 references) before treating the skill as live. Until confirmed, `personas/validators/` + `personas/VALIDATOR_PROTOCOL.md` remain the operative validator system.

**Next action when resumed:** ask Ignat if he ran the command, or proactively check `skills/design-review/` on disk to see if it now exists.

## Clarification needed 2026-08-03 13:21 UTC — "which server?" — Option A may not be practical

Ignat asked "which server? I don't understand" in response to the install instructions — revealed he may not have terminal/SSH access to the machine OpenClaw runs on.

**Clarified:** "the server" = the Linux machine running OpenClaw itself (hostname `openclaw-ignat-plotnikov`, Ubuntu) — where the workspace, prototypes, and all files live. Not his Mac/local machine. Running the install command requires SSH/terminal access to that specific box.

**Revised framing given:** if Ignat doesn't have easy terminal access to that machine, Option A isn't practical, and **Option B becomes the real answer** — he says "turn the approval off," I set `skills.workshop.approvalPolicy: "auto"`, and I install the skill myself from here (one-line, reversible config change).

**STATUS as of 13:25 UTC: awaiting Ignat's answer to "do you have shell access to that machine, or should we go with B?"** Nothing installed. `design-review` skill (proposal ID `design-review-20260803-64e918e398`) still pending. This is now the single most important open thread — likely resolves to Option B given the confusion, but waiting for his explicit confirmation before touching `skills.workshop.approvalPolicy`.

**Next action when resumed:** check if Ignat answered the access-vs-Option-B question. If he says "turn the approval off" (or clear equivalent), set `skills.workshop.approvalPolicy: "auto"` via the `gateway` tool (config.schema.patch/apply), restart gateway, then run the install myself and verify `skills/design-review/` exists with all 7 files before confirming to him it's live.

## RESOLVED 2026-08-03 13:25-13:30 UTC — kept old way, no skill install needed

Ignat's situation: he has terminal on his MacBook only; the actual OpenClaw server is controlled by IT, not him. Asked if Option B (auto-approval) would break anything, and whether we could just continue the old way instead.

**Decision made: skip the skill migration entirely, stick with the personas/ system, but fix it properly in git.**

**What was actually done (committed as `a6fb7c7`):**
1. **`AGENTS.md` updated** — now opens with a "Design Reviews" pointer section instructing to read `personas/VALIDATOR_PROTOCOL.md` before any review, with an explicit note about the 2026-07-31 mistake (claiming validators didn't exist) so it can't happen again. This is read every session automatically — solves the discoverability problem WITHOUT needing the skill system or any approval flow.
2. **Trust Validator added**: `personas/validators/trust-validator.md` — the "does the UI lie about its own state?" pass, 5th validator alongside UX/Domain/Clarity/Fidelity.
3. **`personas/KNOWN_FRICTION.md` created** — KF-1 through KF-9 real LogRocket findings (Reload button rage-clicks, 97.2% import→sync drop-off, date-picker arrow spam, dead stepper labels, etc.) — validators can now check designs against measured behavior.
4. **Trigger fixed directly in `VALIDATOR_PROTOCOL.md`** — added target-resolution logic (Step 0 equivalent) so "review this" works without a pasted URL, resolving from conversation context first.
5. **Git housekeeping**: discovered `personas/` had **never been committed to git** — existed only on server disk, meaning if that machine were ever rebuilt the entire validator system would have been lost permanently. Now committed and pushed. Also fixed a stale/expired token in the git remote URL that was silently causing push failures.

**Skill proposal `design-review-20260803-64e918e398` — abandoned, left inert.** Not installed, not rejected (can't reject without approval route either), just ignored going forward. No `skills.workshop.approvalPolicy` change was made — Option B was never actioned since this alternative (fix personas/ in place + commit to git + AGENTS.md pointer) fully solved the underlying problem without needing any approval flow at all.

**Outcome: this closes out the entire validator-discoverability saga from this session.** No further action needed on the skill install front. The live, correct, git-backed system is:
- `personas/VALIDATOR_PROTOCOL.md` — protocol, now includes Step-0-equivalent target resolution
- `personas/validators/` — UX, Domain, Clarity, Fidelity, **Trust (new)**
- `personas/KNOWN_FRICTION.md` — new, KF-1 through KF-9
- `personas/DESIGN_RULES.md` — style rules (still has some stale info per earlier notes — token file and synder-components.html are the real truth for colors/type scale)
- `AGENTS.md` — now points here explicitly at the top, read every session

## Session close-out note

This was a long session focused entirely on fixing the validator/review-system discoverability problem, correcting a mistaken claim, and exploring (then abandoning in favor of a simpler fix) a skill-based migration. Net result: the personas/ system is now git-committed, has 5 validators instead of 4, includes real LogRocket friction data, and is guaranteed to be read every session via the AGENTS.md pointer. This is a durable fix — no dependency on OpenClaw's skill/approval system at all.

**No open TODOs remain from this thread.** Earlier open items from before this session (component library icon fixes, weekly LogRocket cron query spacing, oracle/dataviz skill adoption decision, monthly deep-dive cron) are still genuinely open and should be checked in a future session, but were not the focus of today's work.

## Final confirmation to Ignat 2026-08-03 13:30 UTC

Ignat asked plainly "Is everything okay now?" Confirmed: yes, everything works, no action needed from him. Told him simply: say "review this" while working on a screen, no link needed; the 5th (Trust) validator now catches UI-lying issues; validators know real LogRocket problems now; the skill idea was dropped as unnecessary complexity.

**This closes the validator/review-system saga for this session — confirmed resolved and understood by Ignat.**

## Session end-of-day state (2026-08-03)

Everything from today is now in this file. Summary of durable state going forward:

- **Live validator system**: `personas/VALIDATOR_PROTOCOL.md` + `personas/validators/` (UX, Domain, Clarity, Fidelity, Trust) + `personas/KNOWN_FRICTION.md` (KF-1 to KF-9) + `personas/DESIGN_RULES.md`. All committed to git (commit `a6fb7c7`). `AGENTS.md` points to this at the top of the file, read every session — this is the fix for the discoverability problem.
- **Trigger**: "review this" / "review" / "check this" work directly on whatever's currently being discussed — no URL required, resolved via conversation context.
- **Skill proposal abandoned**: `design-review-20260803-64e918e398` — inert, not installed, not needed. Do not revisit unless Ignat brings it up again.
- **No approval-policy changes made** — `skills.workshop.approvalPolicy` untouched, still default.

**Genuinely still-open items carried forward from BEFORE today's session (not addressed today, still pending for a future session):**
1. Weekly LogRocket cron — space the 3 Galileo queries further apart to avoid rate-limiting (drop-off/error data didn't come through in the first run 2026-07-27)
2. Component library (`reports/synder-components.html`) — sidebar icons still emoji placeholders, need real Material Icons; table row density unverified; missing components: modals, tooltips, breadcrumbs, date pickers, toast notifications
3. Decision never made: adopt `oracle` skill (second-model review) and/or a dataviz/charting skill for the weekly friction report
4. Decision never made: build the proposed monthly deep-dive cron (LogRocket + Jira + component-library cross-reference)
5. Push an actual fix for the recurring rage-click friction point (Daily Summaries "Synced" filter + "Apply filter" button) — confirmed real via LogRocket, corroborated by manual finding, no design fix made yet

Next session: read this file + MEMORY.md + PROJECTS.md as usual; the validator saga is closed, pick up one of the 5 items above if Ignat wants to continue.

## Settings & Billing — round 2: Manage subscription v2 (13:34–13:50 UTC)

Ignat: "Let's continue with manage subscription." He didn't pick a layout and didn't answer
my earlier AskUserQuestion, so I proceeded with my stated recommendation (**B**) and said so
explicitly rather than stalling.

**Built** `projects/settings-rework/manage-subscription-v2.html` — the billing screen at
production depth. Live:
https://dashasyn.github.io/synder-prototypes/projects/settings-rework/manage-subscription-v2.html

- **9 switchable states:** active, trial, past due, cancelling, expired, Premium (quoted),
  no card on file, loading (skeletons), billing unavailable
- **2 org scenarios:** the captured demo org, and a Pro org with 3 people + 6.9k–9.6k tx/mo
- **Downgrade validation** — blocks changes that strip seats people occupy, disable configured
  smart rules, or drop the ceiling below recent peak. Each names the consequence + one-click fix.
- **Proration with the loss stated** — a mid-term downgrade forfeits unused paid balance
  (their terms say non-refundable), so it's a red line item, the confirm button demotes to
  "Downgrade now anyway (lose $864.90)", and "Schedule it for renewal instead" becomes primary.
- **Usage-based right-sizing** — six months of volume vs the included line. Same component
  recommends a *smaller* bundle on the over-provisioned demo org and an upsell on a busy one.
- **Real cancel flow** — 8-reason taxonomy + sub-reasons recovered from the April capture
  (`.synder-state/unsub-flow/S4-survey-main-reason.png`, `S5-sub-reason.png`), per-reason
  retention offers, final screen spelling out exactly what's lost.

**New findings:** CUR-9 mid-term downgrade silently forfeits paid balance · CUR-10 no downgrade
validation exists · CUR-11 billing page never shows usage so it can never advise paying less ·
SPEC-5 eight of nine states are my proposal, dunning timings and trial length unverified.

**My own bugs, found by testing and fixed:**
- FIX-7 — I hardcoded the current monthly total at 92 next to quantities that disagreed, so
  reducing seats showed "+$0.00". Replaced with a single `priceOf(config)` used everywhere.
- FIX-8 — both downgrade guardrails were unreachable: the demo org uses 240–420 of 1,000
  transactions and sits at its plan's seat minimum. Untestable code is unreviewable design,
  so usage/seats became scenario data and I added the Pro-org scenario.

**Lesson worth keeping:** when a safeguard can't be triggered in the demo data, it can't be
reviewed either — build the scenario that exercises it, or the design decision is invisible.

**Still blocking:** SPEC-1 (real add-on rates), SPEC-2 (Member role vs the invite modal),
SPEC-5 (dunning schedule), and which of A/B/C to take forward.
