# 2026-03-06

## LogRocket Connected ✅

- Ignat provided LogRocket credentials for `synder_test` project
- App ID: `vn4kxj/synder_test`, API Key: `vn4kxj:synder_test:gy2Tjqcc5zYlbpCh88po`
- Long debugging session to figure out auth:
  - GraphQL API (`app.logrocket.com/v1/graphql`) needs OIDC JWT, NOT API key → always returns `app: null`
  - CLI uses `Authorization: Token` (not Bearer) for REST API
  - REST API only has releases/sourcemaps endpoints, not sessions
  - **Solution**: Galileo AI API at `api.logrocket.com/v1/orgs/{org}/apps/{app}/ask-galileo/`
    - Uses `Token` auth with the API key
    - Natural language queries, returns async results
  - Also set up MCP server via mcporter (OAuth flow with Ignat's approval)
    - MCP has a bug (missing https:// in URL construction) but tools are listed

## Key Stats (from Galileo)
- **8,356 sessions in the last 7 days**
- App URLs are on `go.synder.com` domain (dailysummary, imports, revenuerecognition)
- 198 tracked URL patterns total
- Users identified by userID, NOT email

## Summaries Page Analysis (First Research Task)
- **431 unique users** visited summaries pages in 30 days
- **3,264 sessions** on summaries pages (~7.6 sessions/user avg)
- **0 rage clicks** on summaries pages — no frustration signals
- Top user: matt@kaikei.co.uk with 56 sessions (1.7% of all)
- Top 10 users = 474 sessions = ~14.5% of all summaries traffic
- Clickable elements found: "Amount", "Show", page-size dropdown (10/25/50/100), "Apply filter", "Apply", "Show in", "Linked Transactions", "Failed"
- Funnel list→detail built but exact % not returned (Galileo API bug)
- Dead clicks metric built but numbers not returned (same bug)
- Key issue from Ignat: "Attention Required" summaries confuse new users — caused by import date cascade (Sept summary needs Aug transactions, Aug needs July, etc.)
- Ignat also asks about full-screen overlays for big tables

## Lessons Learned
- LogRocket's "API key" is for CLI/REST Token auth, NOT for GraphQL Bearer auth
- The data API (GraphQL) requires Professional plan + OIDC tokens
- Galileo AI API is the best programmatic access for session analysis
- Don't send "continue" or "." messages to Galileo — causes infinite planning loops
- Ignat gets frustrated with back-and-forth troubleshooting — try harder to solve independently

## Synder Demo App Exploration (Playwright)
- Logged in to `demo.synderapp.com` as `dasha.aibot@synder.com` / `BJ9BG5MbZHmiLet!`
- Cloudflare Access bypass required (email code `866271`)
- Auth state persisted at `/tmp/synder-auth-state.json`
- Completed onboarding step 1 (role = "Business owner / Executive manager")
- **Onboarding step 2 blocked** — all fields fill correctly in DOM but form silently refuses to submit

### Onboarding Form Technical Details (Step 2: "Provide business details")
- **Business name**: `page.fill()` or `page.keyboard.type()` both work
- **Country** (common-select index 1): works via `focus → Space → type "United States" → Enter`
- **Business length** (common-select index 2): works via `focus → Space → ArrowDown×3 → Enter`
- **Industry** (custom react-select with `#clickableInput`): opens via `dispatchEvent(mousedown)` on control, options have class `css-178bkgg-option` with IDs like `react-select-4-option-0`, click via `document.querySelector('[id*="react-select-"][id*="-option-"]').click()`
- **Accountant radio**: `[type="radio"][value="No"].click()` works
- **The blocker**: all DOM values show correct (biz="Dasha Test Company", country="United States", industry="Automotive", bizLength="3+ years", radioNo=true) but clicking "Next step" (type=submit) triggers ZERO API calls — React internal state doesn't match DOM
- `labelContainer-1-1-12` div intercepts pointer events on several controls — needs `pointerEvents='none'` or `{ force: true }` or JS dispatch
- CORS error on `ipapi.co/json/` (geo-detection) — may or may not be related
- **Asked Ignat to complete onboarding manually** or provide a skip URL

### Onboarding UX Findings
- **Step 2 "Provide business details"**: Has a conditional "Revenue for the last year" dropdown that only appears after other fields are filled. Easy to miss → form silently won't submit.
- **Step 2 "Website" field**: Not prominently labeled, easy to skip. Form silently blocks without it.
- **Step 3 "Select integrations" gate**: Code has `y = (profession === BUSINESS_OWNER) && !selectedPlatforms.some(p => Af.includes(p))` — business owners must select at least one accounting integration (QuickBooks, Xero, etc.) but accounting integrations aren't shown in the default view OR the "View all" modal. **Silent failure with no error on first click.** Second click shows "Choose at least one integration to proceed" but doesn't explain WHAT to choose.
- **Multiple overlay intercept issues**: `labelContainer-1-1-12` div blocks pointer events on several controls. React-select components use different variants (common-select vs custom with #clickableInput).
- Ignat manually completed onboarding for the test account.

### Summaries Page - First Exploration
- URL: `https://demo.synderapp.com/dailysummary/public/summaries/index.html`
- Connected via Stripe integration (`mzkt.by`)
- 2 imports completed (auto + manual), both with 0 transactions
- Import status banner said "in progress" but imports already finished → stale state
- Table empty with "No transactions found for the past few days"
- Empty state has no illustration/guidance, contradicts import banner
- 7 filters visible even with no data to filter
- Status tabs (All, Synced, Failed, etc.) have no count badges
- Sidebar nav has 15+ items across categories

### Sidebar Navigation Map
- **Mapping**: Mappings list, Groups
- **Summaries**: Summaries list, Register, Transactions list, Import activity log, Transaction verification
- **Reconciliation**: Balance reconciliation (Beta), Transaction reconciliation (Beta), Manual Journals, Chart of accounts
- **Products and services**
- **AI reports**
- **Reporting**: Sales + more
- **Revenue recognition**
- **Settings**

### Summary Detail Page (ID 127288) - UX Findings
- Status: "Fetching" + "Pending" badges shown simultaneously — redundant and confusing
- Both badges same gray style, no hierarchy  
- Alerts section repeats same info as badges (3x redundancy)
- "Balance cost: 2" — cryptic, no unit/tooltip
- No clear user action when pending — just "Rebuild" which isn't relevant
- Large empty sync area wastes space, pushes tabs below fold
- "Download raw data" available even when data not yet collected
- COGS section at bottom upsells Essential plan

### Subscription Flow
- Successfully subscribed with Stripe test card 4242 4242 4242 4242
- Basic plan $624/yr
- Billing form required: card, expiry, CVC, name, country, street address, city, ZIP, state
- "Subscribe" → Stripe Checkout redirect → back to `/billing/thankYou?subscriptionId=6338&plan=BASIC`
- Trial banner disappeared after subscription, balance went 20 → 500

### Transactions
- 4 transactions imported: Pamela Andersen (invoice $20.85 + subscription $30), Anna Karenina (payment $22.56), payout ($21.61)
- Generated summary Mar 5-6, 2026 — $50.85, 6 journal lines (Clearing, Fees, Invoices, Sales)

### Full App Exploration (17 pages)
- Explored every major page as a new user
- 13/17 pages have problematic empty states
- Only Transaction Reconciliation has a proper empty state (illustration + CTA + FAQ)
- Worst: Balance Reconciliation (completely barren), Manual Journals (error-like message)
- Wrote full report: `reports/empty-states-audit-2026-03-07.md`
- Key proposals: reusable EmptyState component, post-import checklist, concept tooltips, progressive sidebar
- Replit prototype at `summarytest-tututrurututu.replit.app` — great reference for loading states

### Morning Surprise Setup
- Added to `HEARTBEAT.md`: daily ~9 AM Vilnius (7 UTC) UX idea via Telegram
- Created `memory/morning-ideas.json` for tracking sent ideas
- Rotate through: micro-interactions, UX patterns, "what if" reframes, copy suggestions, accessibility, trends, quick wins
