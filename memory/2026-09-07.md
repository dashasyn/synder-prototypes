# 2026-09-07

## Banner builder added to the prototype hub (Ignat, 07:42)

The hub is `projects/index.html` — "Ignat's Prototype Hub". Note for future me: the *other* two
index files are stale decoys — root `index.html` still says "Prototypes · 18 items" and
`prototypes/index.html` stops at welcome-back-*. Recent projects (payment-application v2–v4,
provider-filter-modes, sync-details) were never added to any of them, so the hub lags the work.

Added the banner builder under a **new "Campaigns" sub-group** in the Synder Prototypes section —
it is not settings, monitoring, a dashboard, a flow or a report, and "Reports & Tools" holds only
old `../reports/` pages. Verified live incl. click-through: `scripts/check-hub-banner-card.cjs`.

Two test-harness lessons:
- A `file://` link to a **directory** (`./banner-builder/`) loads Chrome's directory listing, whose
  own script throws `start is not defined`. A click-through assertion is only meaningful against
  the published URL; locally assert the href plus the target file on disk.
- Lucide **keeps the `data-lucide` attribute on the generated `<svg>`**, so "no `[data-lucide]`
  left" is not the right test for a bad icon name. The real signal is a surviving `<i data-lucide>`.

Found while there: the hub's `github` link icon never renders — that name was dropped from lucide's
core set. Pre-existing and decorative; reported, not fixed, since it was outside the ask.

## End-of-day snapshot (2026-09-07)

**Short day — two commits, both before 08:00 UTC, nothing after.**

| Time | What |
|---|---|
| 03:00 | Overnight dream/reflection pass (automated). `MEMORY.md` **still untouched since 09-04** — third cycle with nothing promoted to long-term. Now worth acting on rather than just noting. |
| 07:08 | Morning idea sent (copy/tone, entry 105) — the music prototype's apply-to strings still count *stations* (`Apply to ${n} station(s)`, lines 759/1019) after round 2 re-keyed the model to **station + line**. So "Apply to 3 stations" can mean three platforms of one Alexanderplatz, and the overwrite warning counts the same wrong unit. Copy-only fix; no reply yet. |
| 07:45–07:46 | Banner builder added to the prototype hub under a new **Campaigns** sub-group, verified live with click-through (`3fec035`), then logged (`ed18bde`). |

### The finding worth more than the task it came from

**The hub lags the work, and there are three competing index files.** `projects/index.html` is the
real one; root `index.html` still claims "Prototypes · 18 items" and `prototypes/index.html` stops
at `welcome-back-*`. Payment-application v2–v4, provider-filter-modes and sync-details were **never
added to any of them**. So the canonical entry point under-reports roughly a week of work — anyone
Ignat sends to the hub sees a stale picture. Adding one card today fixed the symptom, not that.

### Open — needs Ignat

Nothing closed today. Unchanged from `memory/2026-09-06.md`, with two counters moved and one new item:

1. **Cloudflare Access service token — now seven days rejected.** PT-settings QBO toggle-order pass
   still not started. Still the only blocker that is about access rather than an answer.
2. **NEW: should the hub be back-filled?** Five-plus recent prototypes are missing from it, and the
   two stale index files are arguably worth deleting rather than maintaining. Small job, but it is a
   decision about what the hub is for, so it is his call, not mine.
3. Carried: banner-builder tie-break confirmation (*later start wins* + overlap warnings); the two
   tutorial-copy problems ("click Explain" shown to a reader already on Explain, and *Cancelled* vs
   `vocabulary.md:144`'s *Canceled*); PT-settings rule 6; A5 and A6; payapp v4's missing validator
   round; and the `SYSTEM_AS_BUILT.md` §6 gaps.
4. Reported but not fixed, outside the ask: the hub's `github` link icon never renders — that name
   was dropped from lucide's core set. Decorative and pre-existing.

**Uncommitted:** `DREAMS.md`, `MEMORY.md`, `memory/morning-ideas.json`, the banner-builder and
hub screenshots, earlier ETC message-generator edits, and the two untracked round directories
from 09-02.

## ETC · Radio / Juke box — two decisions from Ignat (18:04)

He calls the feature **Radio/Juke box** now; it is *Everrunning Musik* in the prototype. Asked
whether that is the client's name — if so the nav label and page title should follow.

- **Licensing is closed, not deferred:** "Licenses are not important." Drop the GEMA question, the
  per-track licence reference/expiry idea and the play-out log from the open list.
- **Priority instead of an overlap ban:** 1. Single track · 2. Playlist · 3. Radio. Overlaps are
  allowed and resolved by that order; **only radio vs radio warns**.
  ⚠️ This contradicts what is built — V2 refuses every overlap on save. He said **"Don't build.
  I will provide more details"**, so nothing was changed; the note is in `PROJECTS.md`.

Wrote both to PROJECTS.md rather than answering from memory next session.
