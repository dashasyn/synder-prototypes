You are running ONE validator lens in Dasha's validator round (VALIDATOR_PROTOCOL v2).

SYSTEM ROLE — read this file in full and obey it exactly as your system prompt:
  /home/ubuntu/.openclaw/workspace/personas/validators/ux-validator.md

INPUTS (read all of these):
  - Round brief:      /home/ubuntu/.openclaw/workspace/projects/q-explorer-prototype/review-2026-08-27/BRIEF.md
  - State map:        /home/ubuntu/.openclaw/workspace/projects/q-explorer-prototype/review-2026-08-27/round2/statemap.json   <-- your primary input, you get the WHOLE map
  - Resolved log:     /home/ubuntu/.openclaw/workspace/projects/q-explorer-prototype/findings-log.json (empty = cold round)

PRIMARY TASK UNDER REVIEW:
  An analyst opens a finished evaluation, reads the quality figures, breaks them down, checks a chart, drills into the raw data and exports.

PROTOTYPE (live, already served): http://127.0.0.1:8099/index.html
  Use a real browser (Playwright, /usr/bin/chromium-browser, headless) ONLY when the state map
  does not record the behaviour your lens needs. Do not re-read the raw HTML wholesale.
  Login: click "Anmelden" (no credentials needed).

DO NOT re-report the AUTO- findings already caught by the deterministic pass:
  - AUTO-5 (raw data views — column filters): 27 column search inputs (16 in view-raw-punct, 11 in view-raw-connection) and 3 selects in view-raw-punct carry no oninput/onchange/onkeyup attribute 
  - AUTO-6 (report V5 — Aufschlüsseln nach): #rpt-aufschluss-1/-2/-3 have no onchange attribute, which is why changing the breakdown leaves the table unchanged. The report's main analysis control
  - AUTO-7 (Ausfallmaske V10 — filter actions): The 'Anwenden' (apply) and 'Zurücksetzen' (reset) buttons in view-ausfallmaske have no onclick attribute. The mask's whole filter form has no working 
  - AUTO-8 (chart popup + Fahrtausfälle toolbar): Buttons with no onclick: CSV Export on view-report-fa, the treemap camera button on view-report-fa, and the Snapshot + fullscreen buttons in the Pünkt
  - AUTO-9 (i18n dictionaries): 37 data-i18n keys used in the markup exist in NEITHER the en nor the de dictionary, so those elements never translate and keep their hardcoded German 

KNOWN ISSUES TO IGNORE: see the "Known issues to ignore" section of BRIEF.md. There is no spec,
so do not report fidelity-to-spec problems.

CAP: at most 5 findings. Confidence floor 70. Prefix UX. round = 2.
Every finding needs the evidence mode your role file demands. The `checked` array is mandatory and
must list everything you inspected — an empty `checked` fails the round.

OUTPUT: write the strict JSON payload (and NOTHING else) to:
  /home/ubuntu/.openclaw/workspace/projects/q-explorer-prototype/review-2026-08-27/round2/ux1.json
Then reply with one line only: "ux1.json: N findings".
